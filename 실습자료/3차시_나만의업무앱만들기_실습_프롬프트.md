# 3차시 실습 — 나만의 업무 앱 만들기 (내가 정한 주제로, 처음부터 끝까지)

> 강의자료: 별도 슬라이드 없음 — **실습 전용 차시** (1·2차시에서 익힌 도구와 개념을 «내 주제»에 적용)
> 주제: **내가 직접 정한 주제**의 «나만의 업무 앱»을 **주제 확정 → 스펙 → 설계 → 구현 → 테스트 → 시연** 6단계로 완성합니다.
> 선행: **1차시 「준비 및 기본 실습」 Part A~C(도구·MCP 준비) 필수** · 2차시 「Azure 서비스 구성과 이해」 권장
> 작업 폴더: `C:\dev\workspace-igm\advanced-cloud-developer` (모든 실습·산출물이 이 아래에 저장)
> 이 문서를 마치면: *"나도 만들 수 있다"* 는 성공 경험과 함께, **교육이 끝나도 혼자 이어갈 발판**을 얻습니다. 주제와 데이터 소스(빈칸)만 바꾸면 **내 어떤 반복 업무에도** 그대로 적용됩니다.

> ⏱️ **진행 흐름 (총 120분)** — 🚀 **먼저 돌려 보고, 그다음 이해합니다**
>
> - **Part A~D · 실습 100분** — 준비 → 주제 정하기 → **통합 프롬프트 1회 실행**으로 0~⑤ 6단계 수행
>   *(6단계 87분 + 예비 13분 — 지연·오류 대비 버퍼)*
> - **Part E · 개념·해설 20분** — 방금 만든 것의 원리와 **프롬프트 라인별 해설**
> - **Part F · 배포 파이프라인(선택)** — dev(k3d) → stg(AKS+Argo CD) → prd(AKS+PaaS) 3환경 배포·테스트
> - **Part G · 심화(선택) +45분** — Advisor Strategy(10‑80‑10) 비용 절감 협업

> 🟢 **두 가지 실행 방법 (공통·한 번만 안내)** — 이 문서의 모든 실습은 아래 두 경로 중 하나로 진행합니다. (결과는 동일)
>
> - **방법 A · Claude Desktop** (GUI, 클릭·대화 중심 — **이 차시 권장**: 앱 실행·시연을 화면으로 바로 확인)
> - **방법 B · Windows Terminal의 `claude` CLI** (터미널, 명령 중심 — 자동화에 강력)
>   🖥️ 본 과정의 CLI 실행은 **Windows Terminal**(Windows 11 기본 탑재)을 표준 터미널로 사용합니다.
>   📁 모든 실습 산출물은 `C:\dev\workspace-igm\advanced-cloud-developer\실습산출물\3차시\` 하위에 저장합니다.

> 🟦 **MCP · 도구** — 1차시 Part A에서 준비한 것을 그대로 활용합니다.
>
> | 도구 | 이 차시에서의 쓰임 |
> | --- | --- |
> | `context7` | 오픈소스 스타터·라이브러리의 **최신 정보·라이선스** 확인 |
> | `Sequential Thinking` | 설계·인수조건의 **단계 추론** |
> | `Playwright` | 완성한 앱의 **UI E2E 테스트·화면 캡처** |
> | `filesystem` | 내 PC의 **파일 읽기·저장** |
> | `xlsx` Skill (또는 openpyxl) | ④ 단계의 **엑셀 테스트 시나리오** 생성 |

> ⚠️ **실습 안전 수칙 (반드시 먼저 읽기)**
>
> - **실제 사내 인사·개인정보·기밀 데이터는 절대 사용하지 마세요.** 민감 주제(HR 등)는 **반드시 가상 데이터**로 진행하고 이름·사번은 가명·마스킹 처리합니다.
> - **외부 데이터를 수집한다면 공개 정보만** 쓰고 각 사이트의 **서비스 약관을 준수**합니다. 수집한 수치·기사에는 **출처와 수집 시각**을 반드시 표기합니다.
> - **비밀값(API 키·비밀번호)은 코드에 하드코딩하지 마세요.** `.env` 또는 설정 파일로 분리합니다.
> - **Docker·k3d 컨테이너는 리소스를 점유합니다.** 실습 후 Part F·H의 정리 절차를 수행하세요. *(클라우드에 배포한 경우 1차시 Part G의 «리소스 그룹 단위 삭제» 원칙을 그대로 따릅니다.)*
> - 캡처 이미지와 엑셀 산출물에도 **실제 개인정보가 남지 않게** 확인합니다.

---

## 🗺️ 개념 한눈에 — "골조는 오픈소스, 인테리어는 내 업무"

> 맨땅에서 벽돌을 굽지 않고, **남이 지어 둔 검증된 골조(오픈소스 스타터)** 에 **우리 업무 인테리어만** 올립니다.
> 다만 **실무 앱**이 되려면 보기 좋은 화면뿐 아니라 **로그인·데이터 저장·엑셀 내보내기·오류 처리·감사로그** 같은
> «회사에서 진짜 쓰려면 꼭 필요한 것»이 들어가야 합니다 — 그래서 **① 스펙 단계에서 이것들을 체크리스트로 못박습니다.**

```
 [오픈소스 스타터(골조)]   +   [실무 요건(인테리어)]   +   [내 업무 데이터]
        │                       로그인·저장·내보내기            │
   검증된 뼈대 재사용            ·입력검증·감사로그          내가 매일 다루는 것
        └──────────────────────────┬──────────────────────────┘
                                   ▼
                        내가 정한 주제의 '나만의 앱'
```

> ✍️ **프롬프트는 '복붙'이 전부가 아닙니다 — 1차시에서 배운 5블록 구조를 그대로 씁니다**
>
> | 1차시 5블록 | 이 차시 통합 프롬프트에서의 위치 |
> | --- | --- |
> | **① 목표 선언** | 첫 문장 — «아래 주제로 나만의 업무 앱을 0~5단계로 만들어줘» |
> | **② 원칙·제약** | `[공통 원칙]` 블록 — 눈높이·보안·데이터 안전·외부 수집 규칙 |
> | **③ 작업 단계** | `[0단계]` ~ `[5단계]` — 각 단계의 «작업» 항목 |
> | **④ 산출물 지정** | 각 단계의 «산출물» 줄 — 파일 경로·형식까지 명시 |
> | **⑤ 검증·안전** | 각 단계의 «검증» 줄 + `[최종 보고]` — 인수조건 O/X 채점 |
>
> 아래 프롬프트의 **한 줄씩 '왜 그렇게 썼는지'** 를 Part E에서 해부합니다. 다음에 다른 앱을 만들 때는 **주제·데이터 소스만 바꿔** 그대로 재사용하세요.

### 🧭 이 차시의 전체 흐름 — 6단계

```
 Part A     실습 준비 — 폴더·도구 점검
      │        "무엇으로 만들 것인가"
      ▼
 Part B     [0] 주제·데이터 소스 확정          ← 빈칸 채우기
      │        "무엇을 · 어떤 데이터로"
      ▼
 Part C     단계별 산출물 체인 이해            ← 앞 산출물이 다음 입력
      │
      ▼
 Part D     통합 프롬프트 1회 실행 → ①~⑤ 수행
      │        ① 스펙 → ② 설계·목업 → ③ 구현 → ④ 테스트 → ⑤ 시연·배포
      ▼
 Part E     개념 & 프롬프트 라인별 해설
      ▼
 Part F     배포 파이프라인 — dev → stg → prd   ← 빌드·배포·구성·테스트 스크립트
      │        로컬 k3d → AKS+ArgoCD(GitOps) → AKS+PaaS(운영)
      ▼
 Part G     (선택) Advisor Strategy 심화
      ▼
 Part H     마무리 — 회고 & 리소스 정리        ← 반드시 수행
```

> 💡 **왜 이 순서인가**: **«무엇이 합격인지»(①)를 먼저 정하지 않으면 ④에서 채점할 기준이 없습니다.**
> 그리고 **«어떻게 생겼는지»(②)를 진짜 화면(HTML 목업)으로 먼저 합의**해야 ③ 구현에서 다시 그리지 않습니다.
> 이 순서는 1차시에서 익힌 «되돌릴 수 없는 결정부터 확정»과 같은 원리입니다.

---

# Part A. 실습 준비 — 폴더 · 도구 점검

> 🎯 **이 파트의 목표**: 통합 프롬프트를 붙여넣었을 때 **중간에 멈추지 않도록** 도구와 폴더를 미리 갖춥니다.
> ⏱️ 예상 시간 **10분**

## 1-1. 준비물 체크 (1차시에서 설치한 것 재사용)

| 도구 | 이 차시에서의 용도 | 확인 방법 | 없을 때 |
| --- | --- | --- | --- |
| **Claude Desktop / CLI** | 통합 프롬프트 실행 주체 | 앱 실행 / `claude --version` | 1차시 1-2 · 1-7 |
| **Node.js (LTS)** | 앱 실행 런타임 | `node -v` | 1차시 1-4 |
| **Docker Desktop** | ② 목업 컨테이너 배포 | `docker version` | 1차시 1-5 |
| **k3d** *(선택)* | 로컬 Kubernetes 배포 | `k3d version` | 없으면 **정적 서버로 대체** |
| **MCP 서버** | context7 · Sequential Thinking · Playwright · filesystem | `claude mcp list` | 1차시 Part A |
| **xlsx Skill / openpyxl** | ④ 엑셀 테스트 시나리오 | `python -c "import openpyxl"` | 프롬프트가 설치 안내 |

> 💡 **k3d가 없어도 실습은 진행됩니다.** ② 단계의 목업은 로컬 정적 서버(`npx serve`)로 열어 캡처하고, 컨테이너 매니페스트는 «만들어만 두는» 방식으로 대체합니다.

## 1-2. ▶ 프롬프트 — 실습 환경 준비 (한 번에)

```text
3차시 실습 환경을 준비해줘. "이미 있으면 건너뛰고, 없으면 설치" 원칙으로 진행하고, 권한이 필요한 작업은 먼저 알려줘.
[원칙]
- 설치·변경 전에 무엇을 왜 하는지 한 줄로 알려주고, 실패해도 중단하지 말고 끝까지 점검한 뒤 한 번에 보고해줘.
- 내 PC의 기존 설정을 덮어쓰지 마.
[단계]
1) 작업 폴더를 만들어줘 — C:\dev\workspace-igm\advanced-cloud-developer\실습산출물\3차시 아래에
   'myapp'(앱 코드) · 'myapp\mockup'(② HTML 목업) · '캡처'(목업·E2E 화면) 폴더,
2) 이 차시는 '내가 정한 주제의 나만의 업무 앱'을 오픈소스 스타터 기반으로 만든다.
   Node.js · filesystem · context7 · Sequential Thinking · Playwright MCP가 준비됐는지 점검해줘,
3) ② 단계에서 HTML 목업을 컨테이너(nginx)로 띄워 볼 거야 —
   Docker Desktop 엔진과 k3d 상태를 확인하고, 없으면 '로컬 정적 서버로 대체'라고 안내해줘,
4) ④에서 엑셀 테스트 시나리오(.xlsx)를 만들 수 있는지 확인해줘(xlsx Skill 또는 openpyxl — 없으면 설치),
5) 1차시에서 만든 산출물(실습산출물\1차시\)이 있으면 목록만 보여줘 — 이번 앱의 입력으로 재활용할 수 있는지 판단하게.
[산출물]
- 점검 결과를 C:\dev\workspace-igm\advanced-cloud-developer\실습산출물\3차시\00_환경점검.md 로 저장.
[검증]
- 폴더 생성 여부와 도구(Node · Docker · k3d · MCP 4종 · xlsx) 준비 상태를
  | 항목 | 기대값 | 실제값 | 결과(O/X) | 조치 | 표로 보고하고,
- X가 있으면 실습 진행에 지장이 있는지(치명/대체가능)를 구분해서 알려줘.
```

**수동 준비 (참고 — Windows Terminal · PowerShell)**

```powershell
cd "C:\dev\workspace-igm\advanced-cloud-developer"
"myapp","myapp\mockup","캡처" | ForEach-Object {
  New-Item -ItemType Directory -Force "실습산출물\3차시\$_" | Out-Null
}
node -v ; docker version ; claude mcp list      # 도구·확장 점검
```

---

# Part B. STEP 0 — 나만의 앱 주제 정하기

> 🎯 **이 파트의 목표**: 통합 프롬프트의 **빈칸 2개**(주제·데이터 소스)를 채웁니다. 여기서 정한 값이 이후 6단계 전체를 좌우합니다.
> ⏱️ 예상 시간 **10분**

## 2-1. 빈칸 채우기 — 이 두 줄이 이번 실습의 전부입니다

```
┌───────────────────────────────────────────────────────────────┐
│  【나만의 앱 주제】 = ________________________________________   │
│                     (아래 추천 중 택1, 또는 본인 업무로 직접)   │
│                                                               │
│  【데이터 소스】   = ________________________________________   │
│                     (a) 화면 입력  (b) 공개 데이터 수집         │
│                     (c) 가상 샘플 데이터  ← 민감 업무는 필수    │
└───────────────────────────────────────────────────────────────┘
```

## 2-2. 추천 주제 4가지 (택1) 또는 본인 업무로 직접

| 추천 | 앱 주제 | 무엇을 해결하나 | 데이터 소스 | 난이도 |
| --- | --- | --- | --- | --- |
| **①** | **팀 업무 요청 관리 대시보드** | 흩어진 업무 요청(메일·메신저)을 한 곳에서 접수·상태 추적·담당자 배정·엑셀 내보내기 | **(a)** 화면 입력 | ★ 쉬움 |
| **②** | **사내 FAQ · 규정 안내 챗봇** | 사내 문서(규정·매뉴얼)를 근거로 질문에 **출처와 함께** 답변 | **(c)** 가상 규정 문서 | ★★ 보통 |
| **③** | **주간 증시 동향 리포트 생성기** | 공개 증시·경제 뉴스를 **주간 단위로 모아** 지수 추이·핵심 이슈·시사점을 **요약 리포트(문서/PPT)** 로 자동 생성 → 임원 보고용 | **(b)** 공개 데이터 수집 | ★★ 보통 |
| **④** | **주간 사내 HR 이슈·현황 대시보드** | **(가상) HR 데이터**로 인원 현황·입퇴사·이슈 건수·부서별 추이를 **주간 대시보드(차트·표)** 로 한눈에 | **(c)** 가상 샘플 데이터 **(필수)** | ★★ 보통 |

> ⚠️ **④ HR 주제 필수 주의** — **실제 임직원 인사 데이터는 절대 사용하지 않습니다.** 반드시 **가상 데이터**로 진행하고 이름·사번은 가명·마스킹 처리합니다. *(교육용 실습이며, 실제 도입 시엔 사내 개인정보 정책·승인 절차를 따라야 합니다.)*
> ⚠️ **③ 증시 주제 주의** — **공개 정보만** 수집하며 각 사이트의 **서비스 약관**을 준수합니다. 생성된 리포트는 **투자 조언이 아닌 동향 요약**이며, 수치는 **출처와 수집 시각**을 함께 표기합니다.

## 2-3. 직접 정할 때의 판단 기준

| 좋은 주제의 조건 | 확인 질문 |
| --- | --- |
| **① 반복적이다** | 매주(또는 매월) 되풀이하는 일인가? |
| **② 입력·출력이 분명하다** | «무엇을 넣으면 무엇이 나오는지» 한 문장으로 말할 수 있는가? |
| **③ 지금은 손으로 하고 있다** | 현재 엑셀·메일·수작업으로 처리하고 있는가? |

> 💡 **한 문장 요령** — *"내가 매주 반복하는 일 중, **자료를 모아 정리해 보고하는 일**"* 이면 대부분 앱이 됩니다.
> *(선택) 1차시에서 만든 산출물(`실습산출물\1차시\`)을 내 앱의 입력 데이터로 재활용해도 좋습니다.*

---

# Part C. 단계별 산출물 체인 — 앞 단계가 다음 단계의 입력

> 🎯 **이 파트의 목표**: 통합 프롬프트를 실행하기 전에 **«무엇이 무엇으로 이어지는지»** 를 먼저 봅니다. 이 그림이 있으면 중간에 결과가 이상해도 **어느 단계로 돌아가야 하는지** 알 수 있습니다.

## 3-1. 입력 → 산출물 체인 (한눈에)

```
 [0]준비·주제·데이터소스  [①]스펙      [②]설계        [③]구현        [④]테스트      [⑤]시연·배포
 무엇을·어떤 데이터로     무엇이 합격   어떻게 생겼나    실제로 동작     합격 판정      남에게 보여주기
 ──────────────       ────────    ─────────     ─────────    ─────────    ──────────
 입력: 내 주제+소스      입력:[0]     입력:[①]       입력:[②]      입력:[①][③]   입력:[③][④]
       │                  │            │              │             │              │
       ▼                  ▼            ▼              ▼             ▼              ▼
 주제·데이터소스        01_앱기획_   02_앱설계.md   myapp/        03_테스트     캡처/01~04.png
 ·산출물형태 확정       스펙.md      +mockup/       (목업→실제     시나리오.xlsx  04_시연·배포
 폴더 준비             (인수조건6·   (HTML·컨테이너) 연동·코드      03_테스트     가이드.md
                       집계기준)     캡처/mockup_   ·차트/리포트)   결과.md
                                     01~03.png                     캡처/01~04
       └──────────────────┴────────────┴──────────────┴─────────────┴──────────────┘
                 앞 단계 산출물이 다음 단계의 입력 → 마지막엔 '내 손으로 만든 내 업무 앱'
```

## 3-2. 단계별 목적 · 입력 · 산출물

| 단계 | 목적 (무엇을 위해) | 입력 | 산출물 |
| --- | --- | --- | --- |
| **0 준비·주제·데이터** | **무엇을 · 어떤 데이터로** | 내 주제 + 데이터 소스 | 주제·데이터 소스·산출물 형태 확정 · 폴더 |
| **① 스펙** | **무엇이 합격인지** | [0] | `01_앱기획_스펙.md` (인수조건 6 · 집계 기준) |
| **② 설계** | **어떻게 생겼나 + 진짜 화면으로 합의** | [①] | `02_앱설계.md` · **`mockup/`(HTML·컨테이너)** · **`캡처/mockup_01~03`** |
| **③ 구현** | **실제로 동작** | [②] (목업 포함) | `myapp/` (목업→실제 연동·데이터 파이프라인·차트/리포트) |
| **④ 테스트** | **합격 판정** | [①][③] | **`03_테스트시나리오.xlsx`** · `03_테스트결과.md` · `캡처/` |
| **⑤ 시연·배포** | **남에게 보여주고 쓰이게** | [③][④] | `04_시연·배포가이드.md` |

---

# Part D. 통합 실습 프롬프트 — 0~⑤ 6단계를 한 번에

> 🎯 **이 파트의 목표**: 프롬프트 **한 번 실행**으로 6단계 전 과정을 수행합니다.
> ⏱️ 예상 시간 **약 80분** (6단계 진행 + 중간 확인)

## 4-1. 붙여넣기 전 확인

| 확인 | 내용 |
| --- | --- |
| **빈칸 2개** | `【나만의 앱 주제】` · `【데이터 소스】` 를 Part B에서 정한 값으로 교체했는가 |
| **작업 폴더** | Claude Desktop에 `실습산출물\3차시` 폴더가 연결되어 있는가 |
| **페르소나** | 첫 줄의 역할·부서를 본인 상황에 맞게 바꿔도 됩니다(산출물 눈높이가 달라집니다) |
| **중간 개입** | 각 단계 끝에서 «입력 → 산출물 → 검증»을 보고하므로, 방향이 어긋나면 **그 자리에서 교정**하세요 |

## 4-2. ▶ 통합 실습 프롬프트 (그대로 복사 → 붙여넣기)

```text
나는 현업 업무 담당자이고, IT 전문가가 아니야.
아래 주제로 '나만의 업무 앱'을 0~5단계로 만들어줘. 각 단계는 앞 단계 산출물을 입력으로 쓰고,
모든 산출물은 실습산출물/3차시/ 아래에 저장해.

【나만의 앱 주제】= <여기에 정한 주제를 적기 (예: 주간 증시 동향 리포트 생성기)>
【데이터 소스】  = <(a) 화면 입력  /  (b) 공개 데이터 수집(WebSearch·WebFetch)  /  (c) 가상 샘플 데이터>

[공통 원칙]
- 산출물은 비개발자도 읽을 수 있게 한국어·표·도식 위주로.
- 의존성은 "있으면 재사용, 없으면 설치". 가능하면 내 PC에서 바로 도는 구성으로.
- 비밀값은 하드코딩 금지 — .env/config로 분리.
- [데이터 안전] 실제 사내 인사·개인정보·기밀 데이터는 절대 사용 금지. HR 등 민감 주제는 반드시 가상 데이터로 만들고,
  이름·사번은 가명 처리, 그 외 식별정보는 마스킹해. 산출물에 실제 개인정보가 남지 않게 해.
- [외부 수집] (b)를 고르면 공개 정보만 수집하고 각 사이트 서비스 약관을 준수해. 수집한 수치·기사에는 출처와 수집 시각을 반드시 표기.
  증시·투자 관련이면 "투자 조언이 아닌 동향 요약"임을 리포트에 명시.
- 각 단계 끝에 "입력 → 산출물 → 검증 결과"를 한 줄로 보고하고 다음 단계로 넘어가.
- 오픈소스·라이브러리 최신 정보는 context7, 복잡한 설계 추론은 Sequential Thinking을 활용해.

[0단계 · 준비·주제·데이터 소스 확정]  목적: "무엇을, 어떤 데이터로 만들지" 못박기
  입력: 위 【나만의 앱 주제】 + 【데이터 소스】 + 내 반복 업무 상황
  작업: ① 주제를 한 문장으로 정리(누가·무엇을·왜), 지금의 수작업 흐름과 절감 시간 가설을 적고,
        ② 【데이터 소스】를 확정해 구체화 —
           (a) 화면 입력: 어떤 항목을 사람이 입력하나
           (b) 공개 데이터 수집: 무엇을·어디서(WebSearch/WebFetch 또는 공개 API)·얼마나 자주(예: 주간)
           (c) 가상 샘플 데이터: 어떤 항목·몇 건을 만들지 (민감 주제는 반드시 이것)
        ③ 산출물 형태를 확정 — 화면(대시보드/목록) / 리포트 문서(md·docx·pptx) / 둘 다,
        ④ 폴더 myapp·캡처 준비.
  산출물: 주제 한 문장 + 데이터 소스 정의 + 산출물 형태 + 수작업 흐름 + 절감 가설
  검증: 주제가 "반복적·입력출력 분명·지금 손으로 하는 일"인가 · 민감 데이터면 (c)로 정해졌는가

[1단계 · 스펙]  목적: "무엇이 합격인지" — 검수 가능한 기준 만들기
  입력: 0단계 주제·데이터 소스·산출물 형태
  작업: ① 핵심 사용자 스토리 3개("누가 / 무엇을 / 왜")와 성공지표(KPI: 예 "동향 정리 2시간→10분"),
        ② MVP 범위를 '필수 / 나중'으로 나누되, 실무 필수요건을 체크리스트로 포함 —
           로그인·권한(관리자/일반), 데이터 영속 저장, 엑셀/CSV 내보내기, 입력검증, 감사로그(누가·언제·무엇), 개인정보 처리,
        ③ [데이터 규칙] 집계 기준을 정의 — 기간 단위(예: 주간=월~일), 갱신 주기, 중복 제거·결측 처리 규칙,
           (b)면 수집 대상·출처 목록과 실패 시 대체(수집 0건 처리), (c)면 가상 데이터 생성 규칙(항목·건수·분포),
        ④ 검증 가능한 인수조건(Given-When-Then) 6개 (정상 3 + 예외 3) — 예외에 '데이터 없음/수집 실패' 포함,
        ⑤ 적합한 오픈소스 스타터/라이브러리 후보 2~3개를 context7로 확인해 라이선스·유지보수 활성도로 비교
           (리포트형이면 문서·PPT 생성 라이브러리, 대시보드형이면 차트 라이브러리 포함).
  산출물: 01_앱기획_스펙.md
  검증: 인수조건 6개가 O/X로 채점 가능한가 · 후보 표를 보여주고 내 선택을 기다려

[2단계 · 설계 + HTML Mockup 배포·확인]  목적: "어떻게 생겼나" — 그림이 아니라 '진짜 화면'으로 미리 합의
  입력: 01_앱기획_스펙.md
  작업: ① 화면 목록·와이어프레임(텍스트/mermaid)과 화면 흐름도
           — 대시보드형이면 어떤 차트를 어디에(예: 추이=선그래프, 구성비=막대/원), 리포트형이면 리포트 목차·레이아웃,
        ② 데이터 모델(엔티티·속성·관계, mermaid erDiagram) — 기간(주차)·집계 결과를 어떻게 저장할지 포함,
        ③ 기술스택 확정 + 선택 이유 한 줄씩(실행 쉬운 스택 우선),
        ④ API 명세 표(메서드·경로·요청/응답 예·상태코드) — 예: 데이터 수집/생성 실행, 주차별 조회, 리포트 내려받기,
        ⑤ 핵심 기능 1개의 시퀀스 다이어그램(예: '주간 리포트 생성' 또는 '대시보드 갱신'),
        ⑥ [HTML Mockup 제작] 위 설계대로 실제로 열리는 정적 HTML 목업을 myapp/mockup/ 에 만들어 —
           내 주제의 주요 화면 2~3개(예: 대시보드형=메인 대시보드·상세·기간필터 / 리포트형=생성 화면·리포트 미리보기·이력),
           - 순수 HTML+CSS(+최소 JS)로 백엔드 없이 가짜 데이터(더미)를 하드코딩해 완성된 모습으로,
           - 차트는 실제로 그려지게(CDN 없이 동작해야 하면 인라인 SVG나 경량 차트 코드로), 리포트형이면 리포트 레이아웃이 보이게,
           - 화면 간 링크 이동 + 예외 화면(데이터 없음·수집 실패)도 포함,
           - 목업 더미 데이터도 가상 데이터로 (HR 주제는 실제 인사정보 절대 금지),
        ⑦ [Mockup 배포·확인] 목업을 컨테이너 웹서버(nginx)로 띄워 브라우저로 확인 —
           nginx:alpine Dockerfile + k8s 매니페스트를 myapp/mockup/k8s/ 에 만들고,
           빌드 → k3d image import → 배포 → 접속 URL 안내, Playwright로 각 화면을 열어 캡처 →
           실습산출물/3차시/캡처/mockup_01~03_*.png
  산출물: 02_앱설계.md · myapp/mockup/(HTML·Dockerfile·k8s) · 캡처/mockup_01~03_*.png
  검증: 1단계 인수조건·집계 기준이 화면·데이터·API에 반영됐는가 · 목업이 실제 브라우저로 열리는가
  ※ Docker/k3d가 준비 안 됐으면 목업은 로컬 정적 서버(예: npx serve myapp/mockup)로 열어 캡처하고, 컨테이너 명령·매니페스트는 만들어만 둬.

[3단계 · 구현]  목적: "실제로 동작" — 오픈소스 골조 + 실무 요건
  입력: 02_앱설계.md + myapp/mockup/ (2단계에서 합의한 화면)
  작업: ⓪ 2단계 HTML 목업을 실제 화면의 출발점으로 삼아 — 목업의 레이아웃·차트 배치·리포트 구조를 그대로 유지하고,
           더미 데이터를 실제 데이터 파이프라인·API 연동으로 교체,
        ① 선택한 오픈소스 스타터로 프로젝트 골격을 실습산출물/3차시/myapp/ 에 만들고(설정은 .env/config로 분리),
        ② 인수조건을 충족하는 핵심 기능을 구현하되 실무 요건을 포함 —
           데이터 영속 저장(SQLite/파일), 입력검증·예외처리와 친절한 오류 메시지,
           엑셀/CSV 내보내기, 목록 검색·필터, 접근 제어(관리자/일반), 감사로그,
        ③ [데이터 파이프라인] 【데이터 소스】대로 구현 —
           (a) 입력 폼 / (b) 수집 모듈(WebSearch·WebFetch, 출처·수집시각 저장, 실패 시 재시도·0건 안내)
           / (c) 가상 데이터 생성기(스펙의 규칙대로, 재실행해도 일관되게),
        ④ [산출물 생성] 대시보드형이면 차트 렌더링(주차별 추이·구성비 등), 리포트형이면 문서/PPT 생성(제목·요약·표·차트·출처),
        ⑤ 개인정보·민감정보 마스킹 원칙을 코드·주석에 반영 (HR 주제는 가명·마스킹 필수),
        ⑥ 현실적인 샘플 데이터 20건 이상(또는 최근 2~4주치)을 넣어 실행 즉시 시연되게,
        ⑦ 설치→실행→접속과 환경변수 설명을 myapp/README.md에 정리(라이선스·보안 주의점 포함).
  산출물: myapp/ (코드 · README.md · 샘플 데이터 · (리포트형) 생성된 리포트 샘플)
  검증: npm start(또는 안내한 명령)로 브라우저에서 실제 동작하고, 차트/리포트가 데이터대로 나오는가

[4단계 · 테스트]  목적: "합격 판정" — 인수조건 대비 O/X
  입력: 01_앱기획_스펙.md(인수조건) + myapp/
  작업: ① [엑셀 테스트 시나리오] 모든 테스트 시나리오를 엑셀로 먼저 작성 —
           실습산출물/3차시/03_테스트시나리오.xlsx (xlsx Skill 또는 openpyxl, 폰트 맑은 고딕),
           시트 4개로 구분: [단위] · [통합] · [E2E] · [요약]
           각 시트 공통 컬럼:
             TC-ID · 테스트구분 · 관련 인수조건(AC-ID) · 시나리오명 · 사전조건(Given) · 실행절차(When) ·
             기대결과(Then) · 테스트데이터 · 우선순위(상/중/하) · 자동화여부 · 결과(Pass/Fail) · 비고
           - [E2E] 시트에 '캡처 파일명' 컬럼을 추가해 각 TC가 어떤 캡처와 대응되는지 명시,
           - [요약] 시트에 구분별 케이스 수·인수조건 커버리지(전체 6개 AC 대비)를 수식으로 집계,
           - 결과 열은 비워 두고 조건부 서식(Pass=초록/Fail=빨강),
        ② 핵심 로직 최소 자동 테스트 2~3개(단위) — 집계·기간 필터·중복 제거 등 주제의 핵심 계산,
        ③ Playwright로 UI E2E — 엑셀 [E2E] 시트의 TC를 그대로 실제 화면에서 검증
           (예: 대시보드형=기간 선택→차트 표시→내보내기 / 리포트형=주차 선택→리포트 생성→미리보기·내려받기)
           과 예외(데이터 없음·수집 실패·빈 값)를 검증,
        ④ 각 단계 성공 화면을 실습산출물/3차시/캡처/ 에 저장 (엑셀의 '캡처 파일명'과 일치하게):
           01_초기화면.png · 02_실행·입력.png · 03_결과확인(차트·리포트).png · 04_예외처리.png
        ⑤ 엑셀의 '결과(Pass/Fail)' 열을 실제 실행 결과로 채우고([요약] 자동 갱신),
           인수조건 6개 대비 통과/실패를 03_테스트결과.md에 표로 정리.
  산출물: 03_테스트시나리오.xlsx(단위·통합·E2E·요약 · 결과 채워짐) · 03_테스트결과.md · 캡처/01~04_*.png
  검증: 인수조건이 모두 통과(또는 실패 원인이 명시)됐는가 · 엑셀 모든 TC에 Pass/Fail이 기록됐는가 · 캡처·엑셀에 실제 개인정보가 없는가

[5단계 · 시연·배포]  목적: "남에게 보여주고 쓰이게"
  입력: myapp/ + 03_테스트결과.md
  작업: ① UI를 한글 라벨·깔끔한 레이아웃으로 정리하고 접근성(키보드·대비) 기본 점검,
        ② 자주 나는 오류를 재현해 1차 방어(경계값·빈 목록·중복) 보강,
        ③ 배포 방법 1개 준비 — 로컬 실행 스크립트 또는 Dockerfile 중 적합한 것(README에 절차 추가),
        ④ '3분 임원 시연 시나리오'(클릭 순서 + 각 화면에서 강조할 KPI)와 변경 요약 정리.
  산출물: 04_시연·배포가이드.md · (선택) Dockerfile
  검증: 처음 보는 사람이 README만 보고 실행할 수 있는가

[최종 보고]
- 0~5단계를 "단계 · 목적 · 입력 · 산출물 · 검증 결과" 표 하나로 요약하고,
- 내 수작업이 이 앱으로 어떻게 바뀌었는지(절감 시간·품질)를 3줄로 정리하고,
- 교육 후 내가 이어서 개선할 것 3가지를 제안해줘.
```

## 4-3. 실행 방법

- **방법 A · Claude Desktop (권장)**: `myapp` 폴더를 연결하고 위 통합 프롬프트를 붙여넣기 → Claude가 0~5단계를 순서대로 수행합니다. 앱 실행·브라우저 확인을 Desktop에 맡겨 **GUI에서 그대로 시연**할 수 있습니다.
- **방법 B · Windows Terminal**:

```powershell
cd "C:\dev\workspace-igm\advanced-cloud-developer\실습산출물\3차시\myapp"
claude                                     # 세션 시작 → 통합 프롬프트 붙여넣기

# 완성 후 (README 안내대로)
npm install ; npm start                    # 또는  pip install -r requirements.txt ; python app.py
npx playwright test                        # UI E2E → ..\캡처 에 단계별 화면 저장
```

> 💡 **잘 안 될 때**: *"에러가 났어 / 화면이 안 떠 / 저장이 안 돼"* 처럼 **본 그대로 말하면** Claude가 원인을 찾아 고쳐 줍니다. 1차시에서 체감한 «구글링 대신 물어보기»를 그대로 적용하세요.

## 4-4. 확인 포인트

| 확인 항목 | 무엇을 보나 |
| --- | --- |
| **산출물 체인** | 주제·소스 → `01_앱기획_스펙.md` → `02_앱설계.md`+`mockup/` → `myapp/` → `03_테스트시나리오.xlsx`·`03_테스트결과.md`·`캡처/` → `04_시연·배포가이드.md` 로 이어졌는가 |
| **HTML 목업(②)** | `mockup/`이 **브라우저로 열리고** `캡처/mockup_01~03`이 저장됐는가 · 목업 화면 구조가 ③ 구현에 그대로 이어졌는가 |
| **실무 요건(③)** | 저장·내보내기·권한·입력검증·감사로그가 **실제로 동작**하는가 |
| **엑셀 시나리오(④)** | `03_테스트시나리오.xlsx`에 **단위·통합·E2E·요약 4시트**가 있고 **결과(Pass/Fail)가 채워졌는가** |
| **E2E 캡처(④)** | `캡처/`에 01~04 성공 화면이 저장됐는가 (엑셀 '캡처 파일명'과 일치) |
| **인수조건** | 6개 중 몇 개가 통과했는가 (`03_테스트결과.md` 표 · 엑셀 [요약] 커버리지) |

---

# Part E. 개념 & 프롬프트 해설

> 🔍 **방금 만든 그 앱이 어떤 원리였는지** 이해합니다. 결과를 이미 봤기 때문에 개념이 훨씬 빨리 붙습니다.
> ⏱️ 예상 시간 **20분**

## 5-1. 오늘 쓴 «핵심 재료» — 그동안 배운 것 총동원

| 재료 | 무엇 | 어디서 준비했나 | 오늘 쓰임 |
| --- | --- | --- | --- |
| **프롬프트 5블록** | ①목표 ②원칙 ③단계 ④산출물 ⑤검증 | 1차시 3-3 | 통합 프롬프트의 뼈대 |
| **Claude Desktop / CLI** | 두 개의 입구, 하나의 엔진 | 1차시 3-1 | 앱 제작의 실행 주체 |
| **MCP 도구** | 외부 도구 연결 | 1차시 Part A | context7(스타터 조사)·Playwright(E2E) |
| **Azure 서비스 이해** | 스토리지·DB·컨테이너 선택 감각 | 2차시 | 데이터 저장·배포 방식 판단 |
| **SDLC 순서** | 요구~배포 체계적 개발 | 이 차시 | 0~⑤ 6단계의 뼈대 |
| **오픈소스 스타터** | 검증된 뼈대 재사용 | 이 차시 | 앱을 «맨땅»이 아닌 «골조»부터 |

## 5-2. «내 앱 만들기» 6단계 — SDLC를 내 주제에 적용

```
 [0] 주제 확정   [①] 스펙      [②] 설계      [③] 구현      [④] 테스트    [⑤] 시연·배포
 무엇을 만들지    무엇이 합격    어떻게 생겼나   실제로 동작    합격 판정     쓰이게 하기
 ──────────    ─────────   ─────────   ─────────   ────────    ─────────
 반복 업무 1개   인수조건 6개   화면·데이터    오픈소스 골조   E2E·캡처     README·시연
 (빈칸)         실무 요건      ·API·스택     +실무 요건     인수조건 O/X  시나리오

 └───── 표준 SDLC의 그 순서를, 이번엔 '내가 정한 주제'로 혼자 굴려 봅니다 ─────┘
```

> 💡 **이 6단계가 재사용 가능한 이유** — 주제만 바뀌었을 뿐 **뼈대는 같습니다.** 다음에 다른 앱을 만들 때도 **주제·데이터 소스(빈칸 2개)만 바꿔** 이 통합 프롬프트를 그대로 쓰면 됩니다. 이것이 **교육 후에도 혼자 이어갈 수 있는 이유**입니다.

## 5-3. 🔬 통합 프롬프트 해부 — «각 블록은 왜 그렇게 썼나»

| 프롬프트의 블록 | 5블록 | 단계 | 왜 이렇게 썼나 (목적) |
| --- | --- | --- | --- |
| "나는 IT 전문가가 아니야 · 한국어·표·도식 위주로" | ② 원칙 | 공통 | **눈높이 지정** — 산출물을 내가 검수할 수 있게 |
| "비밀값은 .env로 분리" | ② 원칙 | 공통 | **보안 기본** — 사내에서 실제로 쓰려면 반드시 |
| **"[데이터 안전] 실제 인사·개인정보 금지 · 가상 데이터로"** | ② 원칙 | 공통 | **사고 예방** — HR 같은 민감 주제에서 가장 중요 |
| **"[외부 수집] 공개 정보만 · 약관 준수 · 출처·수집시각 표기"** | ② 원칙 | 공통 | **합법·신뢰** — 출처가 없으면 보고서로 못 씀 |
| "각 단계 끝에 입력 → 산출물 → 검증을 보고" | ⑤ 검증 | 공통 | **추적성** — 단계가 사슬로 이어졌는지 확인 |
| "지금 손으로 하는 흐름 + 절감 가설" | ① 목표 | 0 주제 | **문제 정의** — 무엇이 줄어드는지 모르면 앱을 만들 이유가 없음 |
| **"【데이터 소스】 (a)입력/(b)수집/(c)가상 확정"** | ③ 단계 | 0 주제 | **주제마다 데이터가 다름** — 이걸 안 정하면 ③ 구현에서 막힘 |
| **"산출물 형태 확정 — 화면/리포트/둘 다"** | ④ 산출물 | 0 주제 | **결과물 모양 합의** — 대시보드인지 문서인지에 따라 설계가 달라짐 |
| "인수조건 6개(정상3+예외3)" | ⑤ 검증 | ① 스펙 | **합격 기준** — ④ 테스트의 «정답지»가 됨 |
| **"집계 기준 — 기간(주간)·갱신주기·중복·결측"** | ② 원칙 | ① 스펙 | **'주간' 리포트의 핵심** — 무엇을 어느 기간으로 묶는지가 정확도를 좌우 |
| "실무 필수요건 체크리스트" | ③ 단계 | ① 스펙 | **실무 앱의 조건** — 화면만 예쁜 앱은 회사에서 못 씀 |
| "오픈소스 후보를 라이선스·활성도로 비교" | ③ 단계 | ① 스펙 | **골조 고르기** — 사내 사용엔 라이선스가 중요 |
| "코드 짜기 전에 화면·데이터·API를 그림으로" | ③ 단계 | ② 설계 | **흔들림 방지** — 설계 없이 만들면 나중에 다 뒤집힘 |
| **"HTML 목업을 만들어 컨테이너로 띄워 확인"** | ⑤ 검증 | ② 설계 | **말 대신 진짜 화면으로 합의** — 와이어프레임은 해석이 갈리지만, 열리는 화면은 오해가 없음 |
| **"목업을 출발점으로 삼아 더미→실제 연동 교체"** | ③ 단계 | ③ 구현 | **설계-구현 일치** — 합의한 화면이 그대로 제품이 됨(다시 그리지 않음) |
| **"[데이터 파이프라인] 소스대로 구현"** | ③ 단계 | ③ 구현 | **데이터가 있어야 앱** — 수집 실패·0건 처리까지 있어야 실무 |
| **"[산출물 생성] 차트 렌더링 / 리포트(문서·PPT)"** | ④ 산출물 | ③ 구현 | **'생성기'의 본체** — 이게 없으면 그냥 목록 앱 |
| "샘플 데이터 20건 이상(또는 최근 2~4주치)" | ③ 단계 | ③ 구현 | **즉시 시연** — 빈 화면으론 아무도 설득 못 함 |
| **"모든 테스트 시나리오를 엑셀로(단위·통합·E2E·요약)"** | ④ 산출물 | ④ 테스트 | **QA 표준 산출물** — 실무는 엑셀 시나리오로 관리·공유·보고 |
| **"엑셀 결과(Pass/Fail) 열을 실행 결과로 채움"** | ⑤ 검증 | ④ 테스트 | **계획과 증거를 한 파일에** — 분리되면 추적이 끊김 |
| "Playwright로 실제 화면 클릭 + 캡처" | ⑤ 검증 | ④ 테스트 | **증거 기반 완료** — "된다더라"가 아니라 화면으로 증명 |
| **"캡처에 실제 개인정보가 없는가"** | ⑤ 검증 | ④ 테스트 | **증거를 남길 때도 안전** — 캡처가 유출 경로가 되지 않게 |
| "처음 보는 사람이 README만 보고 실행" | ⑤ 검증 | ⑤ 배포 | **재현성** — 나만 돌릴 수 있으면 팀에 못 퍼뜨림 |
| "이어서 개선할 것 3가지" | ⑤ 검증 | 전체 | **Step up** — 교육이 끝나도 혼자 이어가게 |

> 💡 **핵심**: 프롬프트의 각 줄은 우연이 아니라 **목적**이 있습니다. 1차시에서 배운 **①목표 → ②원칙 → ③단계 → ④산출물 → ⑤검증** 순서가 여기서도 그대로 작동합니다.

---

# Part F. 환경별 배포 파이프라인 — dev · stg · prd

> 🎯 **이 파트의 목표**: ⑤ 단계에서 «내 PC에서 돌아가는 앱»을 **로컬 → 검증 → 운영** 세 환경으로 올리는 전 과정을, **빌드 · 배포 · 구성 · 테스트(단위·통합·E2E)** 스크립트로 실행합니다.
> ⏱️ 예상 시간 **dev 15분 · stg 40분 · prd 60분** (선택적으로 dev 만 수행해도 됩니다)
> 📘 상세 따라하기: [`배포/가이드/배포_가이드_dev_stg_prd.md`](../배포/가이드/배포_가이드_dev_stg_prd.md)
> 📐 설계 근거: [`설계/README.md`](../설계/README.md) — **`배포/` 는 «어떻게 실행하는가», `설계/` 는 «왜 그렇게 만들었는가»** 를 담습니다.

> 🧭 **이 파트를 읽는 두 가지 방법** — ① **실행 우선**: 6-3 → 6-4 → 6-5 의 명령을 먼저 돌리고, 막히면 각 절의 «설계 요약»으로 돌아옵니다. ② **설계 우선**: [공통 설계 6종](../설계/README.md#2-1-공통-환경-불변--6종)으로 «무엇을 만드는가»를 확정한 뒤 실행합니다. **처음이라면 ①**, 내 주제로 바꿀 때는 ②가 빠릅니다.

## 6-1. 세 환경은 «무엇이 다른가» — 목적으로 나눕니다

> 같은 앱을 세 번 배포하는 것이 아닙니다. **각 환경은 서로 다른 질문에 답합니다.**

| 구분 | **dev** | **stg** | **prd** |
| --- | --- | --- | --- |
| **답하는 질문** | «내 코드가 도는가?» | «**배포 방식**이 맞는가?» | «**장애가 나도 살아남는가?**» |
| 위치 | 내 PC (Docker Desktop) | Azure | Azure |
| 런타임 | **k3d** (경량 Kubernetes) | **AKS** (Free 계층) | **AKS** (Standard · 3개 영역) |
| 이미지 빌드 | `docker build` → `k3d image import` | **`az acr build`** (클라우드 빌드) | `az acr build` + **불변 태그** |
| 배포 방식 | `kubectl apply` (직접) | **Argo CD 자동 동기화**(selfHeal) | **Argo CD 수동 Sync**(사람 승인) |
| 데이터베이스 | in-cluster PostgreSQL | in-cluster PostgreSQL | **PaaS PostgreSQL 유연한 서버**(HA·백업) |
| 캐시·저장소 | 없음 | 없음 | **Azure Cache for Redis · Storage** |
| 비밀 관리 | `kubectl create secret` | `kubectl create secret` | **Key Vault + CSI + 워크로드 ID** |
| **신원 증명** | 없음(로컬은 Azure 밖) | kubelet **관리 ID**(ACR) · WI **전제만 활성화** | **워크로드 ID 로 DB·Redis·Storage 까지 토큰 접근** |
| 외부 노출 | NodePort + 포트 매핑 | LoadBalancer | **관리형 NGINX Ingress + 내부 LB** |
| 쓰기 테스트 | 허용 | 허용 | **금지** (읽기 전용 스모크) |
| 비용 | **0원** | 시간당(낮음) | 시간당(높음) |
| **설계 문서** | [설계/dev](../설계/dev/) | [설계/stg](../설계/stg/) | [설계/prd](../설계/prd/) |

> 💡 **왜 나누는가** — 한 환경에서 세 가지를 다 검증하려 하면 **어느 것도 제대로 되지 않습니다.** dev 는 속도를, stg 는 배포 방식의 정확성을, prd 는 회복력을 각각 책임집니다.

## 6-2. 세 환경 모두 «같은 9단계»를 따릅니다 (MECE 골격)

```
 10 전제조건 → 20 구성 → 30 빌드 → 40 배포 → 50 단위 → 60 통합 → 70 E2E → 80 검증 → 90 정리
 ──────────   ─────    ─────    ─────   ─────────────────────────   ─────   ─────
  도구·권한    인프라    이미지    반영     테스트 3단계 (피라미드)      합격판정  뒷정리
```

| 단계 | 파일명(3환경 공통) | 무엇을 하나 | 빠뜨리면 |
| --- | --- | --- | --- |
| **10 전제조건** | `10_prereq.ps1` | 도구·로그인·권한·이름 규칙 점검 | 뒤 단계가 **중간에 멈춤** |
| **20 구성** | `20_config.ps1` | 클러스터·네트워크·DB·비밀·플랫폼 준비 | 배포할 **«곳»**이 없음 |
| **30 빌드** | `30_build.ps1` | 이미지 생성 + 추적 가능한 태그 부여 | 배포할 **«것»**이 없음 |
| **40 배포** | `40_deploy.ps1` | 매니페스트 반영 + 롤아웃 완료 대기 | 이전 버전이 그대로 |
| **50 단위** | `50_test_unit.ps1` | **외부 의존 없이** 순수 로직 검증 | 계산이 틀린 채 배포 |
| **60 통합** | `60_test_integration.ps1` | **API + DB 연동** 검증 | 연결·설정 오류를 못 봄 |
| **70 E2E** | `70_test_e2e.ps1` | **브라우저로 사용자 경로** 검증 + 캡처 | 화면·흐름 오류를 못 봄 |
| **80 검증** | `80_verify.ps1` | 리소스 상태·버전 일치·환경별 기준 | 「됐다고 착각」 |
| **90 정리** | `90_cleanup.ps1` | 리소스 제거 (되돌릴 수 없음) | **비용이 계속 발생** |

> 🧪 **테스트를 3단계로 나누는 이유** — **단위**(빠름·DB 불필요) → **통합**(실제 DB) → **E2E**(브라우저) 순으로 **느리고 비싸집니다.** 위에서 먼저 걸러내야 아래 단계에서 시간을 낭비하지 않습니다. 이 구조가 4교시 ④ 단계의 «엑셀 테스트 시나리오 [단위]·[통합]·[E2E] 3시트»와 **1:1로 대응**합니다.

## 6-3. dev — 로컬 k3d 개발 환경

> 💰 **비용 0원** · 클라우드 계정 없이도 배포·테스트 전 과정을 반복할 수 있습니다.

```powershell
cd C:\dev\workspace-igm\advanced-cloud-developer\배포\dev
pwsh -File .\run_all.ps1                # 10 → 80 전체를 한 번에
```

| 단계 | dev 에서 실제로 하는 일 |
| --- | --- |
| **구성** | k3d 클러스터 생성(호스트 `8080` → NodePort `30080`) · ConfigMap/Secret · **in-cluster PostgreSQL** 배포 |
| **빌드** | `docker build` (태그에 **git SHA** 포함) → **`k3d image import`** — 레지스트리 불필요 |
| **배포** | `kubectl apply` → 이미지 태그 지정 → `APP_VERSION` 주입 → **롤아웃 완료 대기** |
| **검증** | readyReplicas · 파드 Running · **재시작 3회 미만** · `/healthz` `/readyz` `/version` · **배포 버전 = 빌드 태그 일치** |

접속: **<http://localhost:8080>** · 정리: `pwsh -File .\90_cleanup.ps1`

### 📐 dev 설계 요약 — «반복 속도»를 최우선으로 둔 결과

| 설계 축 | dev 의 선택 | 왜 그렇게 했나 | 문서 |
| --- | --- | --- | --- |
| **아키텍처** | k3d 1노드 · 복제본 1 · in-cluster PostgreSQL(비영속) · NodePort | **비용 0 · 오프라인 가능**, 데이터는 «언제든 버릴 수 있어야» 한다 | [01](../설계/dev/01_아키텍처설계서_dev.md) |
| **프로세스** | `docker build` → **`k3d image import`** → `kubectl apply` | 레지스트리 왕복을 없애 **빌드→배포 사이클을 단축** | [02](../설계/dev/02_프로세스설계서_dev.md) |
| **구성** | `env.dev.json` + 환경 변수 `DEV_DB_PASSWORD`(**기본값 있음**) | 첫 실행에서 «환경 변수부터 설정하라»는 벽에 막히지 않도록 | [03](../설계/dev/03_구성설계서_dev.md) |
| **테스트** | 단위·통합·E2E **전부 실행** · `allowWrite=true` | dev 가 **«기능 합격»의 1차 관문** — 여기서 막힌 것은 stg 로 가지 않는다 | [04](../설계/dev/04_테스트설계서_dev.md) |

**핵심 결정 3가지**

| ADR | 결정 | 버린 것 |
| --- | --- | --- |
| **D-01** | Docker Compose 가 아니라 **k3d** | 초기 설정 간편함 → 대신 **stg·prd 와 같은 `kubectl`·매니페스트**를 그대로 쓴다 |
| **D-02** | 로컬 레지스트리 없이 **`image import`** | 이미지 이력 → 대신 **실패 지점이 하나 줄어든다** |
| **D-05** | 복제본 **1** | 무중단 배포 검증 → **stg 로 미룬다** |

> 🔎 **dev 에서도 비루트(`runAsNonRoot`)와 `resources.requests` 를 유지하는 이유** — 여기서 생략하면 prd 로 옮길 때 «갑자기 안 되는» 문제를 만납니다. **환경 간 차이는 최소화**하는 것이 원칙입니다.
> ⚠️ **dev 에서 «고치기»보다 «다시 만들기»가 빠릅니다.** 클러스터 생성이 수십 초이므로, 원인 분석에 10분 이상 걸릴 상황이면 `90_cleanup` → `run_all` 이 합리적입니다. **prd 는 정반대**입니다.

## 6-4. stg — AKS + Argo CD GitOps 검증 환경

> 🎯 **여기서 검증하는 것은 앱이 아니라 «배포 방식»입니다.** Git 이 유일한 진실이 되고, 클러스터가 그 상태로 **스스로 수렴하는지**를 봅니다.

```powershell
# ⚠️ 먼저 config\env.stg.json 의 acrName(전역 고유) · repoUrl(Git 저장소) 을 변경
cd ..\stg ; pwsh -File .\run_all.ps1
```

```
 개발자              Git 저장소            Argo CD (클러스터 안)          AKS
   │  30_build         │                        │                         │
   │  (ACR 빌드)       │                        │                         │
   │  40_deploy:       │   변경 감지             │                         │
   │  overlay 의       ├───────────────────────►│  선언 상태 ↔ 실제 상태   │
   │  newTag 갱신 →    │                        │  비교 후 차이만 적용     ├──► 배포
   │  commit & push    │                        │                         │
   │                   │                        │  누가 kubectl 로 손대면  │
   │                   │                        │  selfHeal 이 Git 상태로 ├──► 자동 원복
```

| 단계 | stg 에서 달라지는 점 |
| --- | --- |
| **구성** | RG·**ACR**·**AKS**(CNI Overlay·워크로드 ID) 생성 → **Argo CD 설치** → AppProject·Application 등록 |
| **빌드** | **`az acr build`** — 소스를 ACR 로 보내 **클라우드에서 빌드·푸시**(로컬 Docker 불필요) |
| **배포** | overlay `kustomization.yaml` 의 `newTag` 갱신 → **git commit·push** → Argo CD 가 감지·동기화 |
| **검증** | `Synced` · `Healthy` · **배포 이미지 = 최신 빌드 태그** · **selfHeal 드리프트 복구**(레플리카를 강제로 바꿔 보고 자동 복구되는지 실제 확인) |

> 🔑 **CI 는 이미지를 만들고, CD 는 Git 을 본다.** 파이프라인이 클러스터 자격 증명을 갖지 않으므로 공격면이 줄어듭니다. **Secret 은 절대 Git 에 커밋하지 않습니다.**

### 📐 stg 설계 요약 — «배포의 재현성·추적성»을 최우선으로 둔 결과

| 설계 축 | stg 의 선택 | 왜 그렇게 했나 | 문서 |
| --- | --- | --- | --- |
| **아키텍처** | AKS Free 2노드 · **복제본 2 + `maxUnavailable: 0`** · PDB · in-cluster PostgreSQL(PVC) | dev 에서 못 하던 **무중단 롤링 업데이트를 실증** · DB 는 배포 검증에 필요한 만큼만 | [01](../설계/stg/01_아키텍처설계서_stg.md) |
| **프로세스** | `az acr build` → **overlay `newTag` 커밋·push** → Argo CD 동기화 | 배포 명령이 **`kubectl apply` 가 아니라 `git commit`** 이 된다 | [02](../설계/stg/02_프로세스설계서_stg.md) |
| **구성** | **Kustomize base + overlays/stg** · Secret 은 Git 밖 · `STG_DB_PASSWORD` **기본값 없음** | 환경 차이를 «패치»로만 표현 · «비밀은 항상 외부 주입» 습관 강제 | [03](../설계/stg/03_구성설계서_stg.md) |
| **테스트** | 기능 TC 전체 + **GitOps 검증 G-01~05** | 기능 재검증이 아니라 **«같은 이미지가 다른 환경에서도 동일하게 도는가»** 확인 | [04](../설계/stg/04_테스트설계서_stg.md) |

**Push 배포 vs Pull(GitOps) — 무엇이 달라지나**

| 관점 | 전통적 Push | **Pull (stg 채택)** |
| --- | --- | --- |
| 자격 증명 | CI 가 **클러스터 관리자 권한** 보유 | CI 는 클러스터 접근 **불필요** |
| 드리프트 | 수동 변경이 그대로 남음 | **selfHeal 자동 교정** |
| 배포 이력 | 파이프라인 로그 | **Git 이력 = 배포 이력** · `revert` 가 곧 롤백 |

**GitOps 검증 TC — 스크립트가 실제로 확인하는 것**

| TC | 확인 | 실패 시 의미 |
| --- | --- | --- |
| **G-01·02** | `Synced` · `Healthy` | Git 과 클러스터 불일치 · 워크로드 비정상 |
| **G-03** | **배포 이미지 = 빌드 태그** | 커밋이 반영되지 않음(push 누락 등) |
| **G-04** | `readyReplicas ≥ 2` | 자원 부족 · 스케줄 실패 |
| **G-05** | **`kubectl scale --replicas=1` 로 일부러 망가뜨린 뒤 240초 내 자동 복구** | **GitOps 가 작동하지 않음** |

> 🎯 **G-05 가 stg 의 존재 이유입니다.** «누가 `kubectl` 로 손대도 Git 상태로 돌아온다»를 **말이 아니라 실행으로** 증명합니다.
> ⚠️ **stg 에서 `kubectl rollout undo` 는 «작동하지 않는 것처럼» 보입니다.** selfHeal 이 Git 상태로 다시 수렴시키기 때문입니다. **결함이 아니라 의도된 동작**이며, 이것을 체감하는 것이 학습 포인트입니다.

## 6-5. prd — AKS + PaaS 서비스 운영 환경

> 🚦 **전제: stg 검증 통과 기록이 있어야 진행됩니다.** `10_prereq.ps1` 이 `배포\stg\reports\stg_verify_*.json` 의 `fail=0` 을 확인합니다 — **검증되지 않은 것을 운영에 올리지 않는다**는 규칙을 스크립트가 강제합니다.

```powershell
$env:PRD_DB_ADMIN_PASSWORD = "Prd-" + [guid]::NewGuid().ToString("N").Substring(0,16) + "!aZ"
cd ..\prd ; pwsh -File .\10_prereq.ps1
```

**PaaS 선택 — 무엇을 왜 관리형으로 넘겼나**

| 역할 | 선택 | 왜 (2차시 «PaaS 우선» 원칙 적용) |
| --- | --- | --- |
| 관계형 DB | **PostgreSQL 유연한 서버**<br/>(GeneralPurpose · **영역 중복 HA** · 백업 14일 · **공용 액세스 차단**) | 클러스터 안 DB는 백업·HA·패치를 직접 져야 함. PaaS 가 대신하고 **PITR 로 논리적 손상까지 복구** |
| 캐시·세션 | **Azure Cache for Redis** | 세션을 외부화해야 파드를 자유롭게 늘리고 줄일 수 있음 |
| 객체 저장 | **Azure Storage** (ZRS · **공유 키 비활성화**) | 첨부·리포트 보관 · 계정 키 대신 ID 기반 접근 |
| 비밀 | **Key Vault + CSI 드라이버 + 워크로드 ID** | **Git·이미지·앱 설정 어디에도 비밀 값이 없다** |
| 신원 | **사용자 할당 관리 ID + 연합 자격 증명** | 파드가 **비밀번호 없이** DB·Redis·Storage·Key Vault 에 접근 |
| 관측 | **Log Analytics · App Insights · Container Insights · 관리형 Prometheus** | 증상(지연·오류·포화) 기반 경고의 기반 |
| 진입 | **관리형 NGINX Ingress + 내부 LB** | 앱은 내부에만 노출, 외부 진입은 한 곳으로 |

**인프라 생성 순서 — «참조당하는 것부터»**

```
 1 네트워크(VNet·서브넷)  →  2 관측(Log Analytics·App Insights)  →  3 보안(Key Vault·관리 ID)
        →  4 데이터(PostgreSQL·Redis·Storage)  →  5 비밀을 Key Vault 에 저장  →  6 AKS + 워크로드 ID 연합
```

**운영 준비 검증(`80_verify.ps1`)이 보는 것**

| 축 | 통과 기준 |
| --- | --- |
| **가용성** | replicas ≥ 3 · **파드가 2개 이상 영역에 분산** · PDB · HPA |
| **보안** | 워크로드 ID · **불변 태그(`latest` 금지)** · 비루트 + 읽기 전용 루트FS · **DB 공용 액세스 차단** · HA · 백업 ≥ 7일 |
| **관측** | Container Insights 사용 |
| **복구** | 리비전 이력 2개 이상 → **즉시 롤백 가능** (`rollback.ps1`) |

> ⚠️ **GitOps 롤백의 함정** — `kubectl rollout undo` 로 되돌려도 **Git 의 태그는 그대로**입니다. 다음 Sync 에서 문제 버전이 다시 올라갑니다. 반드시 **Git 의 overlay 태그도 되돌려 커밋**하세요.

### 📐 prd 설계 요약 — «가용성·보안·관측·복구»를 최우선으로 둔 결과

> 🔑 **한 문장으로** — «**상태는 밖으로, 비밀은 금고로, 부하는 여러 영역으로, 진입은 한 곳으로.**»

| 설계 축 | prd 의 선택 | 왜 그렇게 했나 | 문서 |
| --- | --- | --- | --- |
| **아키텍처** | AKS Standard(시스템 3 / 사용자 2~6 · **Zone 1·2·3**) · 전용 VNet(앱/데이터 서브넷 분리) · **복제본 3 + HPA 3~12** · PDB 2 · 내부 LB + 관리형 NGINX Ingress | **영역 장애를 견디고**, 앱을 인터넷에 직접 두지 않는다 | [01](../설계/prd/01_아키텍처설계서_prd.md) |
| **프로세스** | 승격 게이트 → ACR 빌드(**불변 태그**) → 커밋 → **사람이 승인 후 수동 Sync** → 검증 → 필요 시 `rollback.ps1` | 반영 시점을 **사람이 결정**하고, 되돌릴 경로를 미리 확보한다 | [02](../설계/prd/02_프로세스설계서_prd.md) |
| **구성** | 설정은 Git(ConfigMap) · **비밀과 접속 대상은 Key Vault → CSI → Secret** · `DB_SSL=true` · `DB_POOL_MAX=10` | 비밀 값이 **Git·이미지·앱 설정 어디에도 없다** | [03](../설계/prd/03_구성설계서_prd.md) |
| **테스트** | 단위 전체 · **읽기 전용** 통합/E2E(`allowWrite=false`) · **운영 준비 검증 P-01~15** | 운영 데이터를 오염시키지 않으면서 **구성이 설계대로인지 감사**한다 | [04](../설계/prd/04_테스트설계서_prd.md) |
| **운영·이행** | 승격 게이트 · 장애 런북 · 비밀 회전 · PITR · 정리 절차 | prd 는 «만들고 끝»이 아니라 **그 이후의 시간**을 설계해야 한다 | [05](../설계/prd/05_운영·이행설계서_prd.md) |

**«비밀번호 없이» Azure 에 접근하는 구조 (워크로드 ID + Key Vault CSI)**

```
 Pod(myapp-sa) ─► CSI 드라이버 ─► AKS OIDC 토큰 ─► Entra ID 교환 ─► Key Vault
                                                                      │
        db-host · db-user · db-password · redis-host ◄─────────────────┘
                          │
        /mnt/secrets 파일 마운트  +  K8s Secret 동기화 → envFrom 주입
```

| 특징 | 의미 |
| --- | --- |
| 파드에 **자격 증명이 없음** | 유출될 대상 자체가 존재하지 않는다 |
| **`DB_HOST` 까지 Key Vault 에** | 접속 대상이 Git 에 남지 않고, **PITR 복원 후 Git 커밋 없이 전환** 가능 |
| 토큰이 짧고 자동 갱신 | 탈취되어도 유효 기간이 제한된다 |

**설계가 만든 «2계층 자동 확장»**

| 계층 | 범위 | 트리거 | 축소 보호 |
| --- | --- | --- | --- |
| 파드(HPA) | **3 → 12** | CPU 65% · MEM 75% | `stabilizationWindowSeconds: 300` |
| 노드(오토스케일러) | 사용자 풀 **2 → 6** | 스케줄 불가 파드 발생 | 기본 정책 |

> ⚠️ **HPA 가 늘려도 노드가 없으면 파드는 `Pending` 입니다.** 두 계층이 함께 있어야 확장이 완성되며, 그 공통 판단 근거가 파드의 **`resources.requests`** 입니다. dev 에서부터 requests 를 적어 온 이유가 여기서 드러납니다.
> ⚠️ **`readOnlyRootFilesystem: true` 와 `/tmp` emptyDir 은 한 쌍입니다.** `/tmp` 를 주지 않으면 앱이 임시 파일을 쓰는 순간 실패합니다.
> ⚠️ **`DB_POOL_MAX` 는 HPA 상한과 함께 계산해야 합니다.** 파드가 12개까지 늘어나는데 풀이 크면 **DB 연결 상한을 순식간에 소진**합니다.

**«없는 것»도 설계입니다**

| 미적용 | 현재 근거 | 필요해지면 |
| --- | --- | --- |
| NetworkPolicy | 진입은 Ingress 로 단일화 · DB 는 공용 액세스 차단 + 전용 서브넷 | `default-deny-ingress` + 명시 허용을 `base/` 에 추가 |
| 이미지 스캔 | 실습 범위 밖 | Defender for Containers 연동 |
| 다중 리전 DR | 단일 리전 설계 | 별도 DR 설계 영역 |

> 📌 **통제가 어디까지 적용됐는지 모호하면, 실제로는 열려 있는데 닫혔다고 믿게 됩니다.** 그래서 설계서는 «적용한 것»뿐 아니라 **«적용하지 않은 것»도 명시**합니다.

## 6-6. 스크립트 폴더 구조

```
배포/
├── README.md                          전체 개요 · 환경 비교
├── 가이드/배포_가이드_dev_stg_prd.md    ← 따라하기 가이드(절차·문제해결·체크리스트)
├── common/
│   ├── lib.ps1                        공통 함수(로깅·검증·리포트·파괴적 작업 확인)
│   └── app/                           공통 샘플 앱 — myapp 이 없으면 자동 사용
│       ├── src/lib/summary.js         순수 로직(단위 테스트 대상)
│       ├── test/unit · integration · e2e
│       └── Dockerfile                 멀티스테이지 · 비루트 · HEALTHCHECK
├── dev/    10~90 + run_all + config/k8s/
├── stg/    10~90 + run_all + config/{k8s(base·overlays), argocd}/
└── prd/    10~90 + run_all + rollback + config/{k8s, argocd, infra}/
```

> 📌 **앱 소스는 자동 결정됩니다** — ① `-AppPath` 지정 → ② `실습산출물\3차시\myapp` → ③ `배포\common\app`(샘플). 실습으로 만든 앱이 준비되면 스크립트가 자동으로 그쪽을 씁니다.

## 6-7. ▶ 프롬프트 — 내 앱에 맞게 배포 스크립트 조정하기

```text
배포/ 폴더의 dev·stg·prd 스크립트를 내가 만든 앱(실습산출물/3차시/myapp)에 맞게 조정해줘.
[원칙]
- 기존 9단계 구조(10 전제조건 → 20 구성 → 30 빌드 → 40 배포 → 50 단위 → 60 통합 → 70 E2E → 80 검증 → 90 정리)와
  파일명은 그대로 유지해. 환경 간 구조가 같아야 비교·학습이 된다.
- 비밀값은 스크립트·설정 파일·Git 어디에도 평문으로 두지 마. 환경 변수 또는 Key Vault 만 사용해.
- 파괴적 명령(삭제)은 반드시 확인 프롬프트를 거치게 유지해.
[단계]
1) 내 앱의 실제 구조를 먼저 읽어줘 — 런타임(Node/Python/…), 포트, 헬스 엔드포인트, DB 사용 여부, 빌드 명령.
2) 그 결과에 맞춰 아래를 수정해줘:
   - 배포/*/config/env.*.json 의 app.containerPort · test.baseUrl
   - 배포/*/config/k8s/**/deployment.yaml 의 프로브 경로·환경 변수·리소스 requests/limits
   - 50/60/70 테스트 스크립트가 호출하는 npm 스크립트 이름(내 package.json 에 맞게)
3) 내 앱에 헬스 엔드포인트(/healthz, /readyz)가 없으면 추가하는 최소 코드를 제안해줘.
   readyz 는 DB 연결을 실제로 확인해야 한다.
4) 내 앱의 단위·통합·E2E 테스트가 없으면, 4단계에서 만든 03_테스트시나리오.xlsx 의
   [단위]·[통합]·[E2E] 시트 TC 를 그대로 코드 테스트로 옮겨줘.
5) dev 환경에서 run_all.ps1 을 실제로 실행해 통과할 때까지 고쳐줘.
[산출물]
- 수정한 파일 목록과 변경 이유를 실습산출물/3차시/06_배포스크립트_조정.md 로 저장.
[검증]
- 인수조건: (a) dev run_all 이 80_verify 까지 전부 Pass
  (b) 비밀값이 어떤 파일에도 평문으로 없음  (c) 9단계 파일명·구조가 유지됨
- 3가지를 O/X 표로 보고해줘.
```

## 6-8. 환경 승격(Promotion) 규칙 — 무엇이 통과해야 다음으로 가나

```
   [dev]                   [stg]                        [prd]
   로컬 k3d                AKS + Argo CD 자동           AKS + PaaS · 수동 승인
   ────────               ──────────────              ─────────────────
   빠른 반복               배포 방식 검증                가용성·보안·복구
   쓰기 테스트 허용         쓰기 테스트 허용              읽기 전용 스모크
        │                        │                            │
        └─ 단위·통합·E2E Pass ───┴─ Synced/Healthy+selfHeal ──┴─ 사람이 Sync 승인
```

| 승격 | 통과 조건 | 확인 위치 |
| --- | --- | --- |
| dev → stg | 단위·통합·E2E 전부 Pass · `dev_verify` fail=0 | `배포\dev\reports\dev_verify_*.json` |
| stg → prd | 위 + **Synced/Healthy** + **selfHeal 복구 확인** | `배포\stg\reports\stg_verify_*.json` (prd 전제조건이 자동 확인) |
| prd 반영 | 위 + **사람의 Sync 승인** + 롤백 경로 확보 | Argo CD UI · `rollback.ps1` |

> 📐 **승격 게이트는 문서상의 규칙이 아니라 스크립트가 강제합니다.** `배포\prd\10_prereq.ps1` 이 `배포\stg\reports\stg_verify_*.json` 을 직접 읽어 `fail=0` 을 확인하고, 아니면 **prd 파이프라인을 시작조차 하지 않습니다.** 설계 근거는 [prd 05 §1 이행(승격) 게이트](../설계/prd/05_운영·이행설계서_prd.md) 참조.

**환경별 «절대 하지 말 것»**

| 환경 | 금지 | 이유 |
| --- | --- | --- |
| dev | 실제 개인정보·운영 데이터 사용 | 로컬은 보호 장치가 없음 |
| stg | **Secret 을 Git 에 커밋** | GitOps 저장소가 곧 유출 경로가 됨 |
| prd | `latest` 태그 · 자동 Sync · 쓰기 스모크 · 무단 `kubectl` 변경 | 추적 불가 · 검증 없는 반영 · 데이터 오염 · 드리프트 |

> 💰 **stg·prd 는 시간당 과금됩니다.** 각 환경의 `90_cleanup.ps1` 을 **반드시** 실행하세요. prd 는 Key Vault 소프트 삭제까지 `az keyvault purge` 로 정리합니다.

---

## 6-9. 보안 강화 — 관리 ID · 워크로드 ID로 «비밀을 만들지 않기»

> 🎯 **이 절의 한 문장**: 비밀을 «**잘 보관**»하기 전에 «**만들지 않는다**».
> 📐 설계 근거: [`설계/공통/07_아이덴티티·시크릿_설계서.md`](../설계/공통/07_아이덴티티·시크릿_설계서.md)
> 🔧 구현: [`배포/common/identity.ps1`](../배포/common/identity.ps1) · [`배포/common/app/src/azureToken.js`](../배포/common/app/src/azureToken.js)

### 6-9-1. 문제 — «비밀번호를 어디에 둘까»는 잘못된 질문일 수 있습니다

```
 흔한 진화 과정                                    남는 문제
 ─────────────────────────────────────────       ──────────────────────
 ① 코드에 하드코딩            →  최악            Git 에 영원히 남는다
 ② 환경 변수로 분리           →  나아짐          누가 볼 수 있나? 회전은?
 ③ Key Vault 에 보관          →  많이 나아짐     회전·감사·만료를 내가 진다
 ④ 비밀 자체를 만들지 않음    →  ✅ 목표         관리할 것이 사라진다
```

| 단계 | 앱이 들고 있는 것 | 유출되면 | 회전 부담 |
| --- | --- | --- | --- |
| ② 환경 변수 | 비밀번호 | **무기한 유효** | 사람이 |
| ③ Key Vault | 비밀번호(주입됨) | 무기한 유효 | 사람이 |
| **④ 토큰(워크로드 ID)** | **짧은 수명 토큰** | **수십 분 내 만료** | **없음** |

### 6-9-2. 세 가지 신원 유형 — 언제 무엇을 쓰나 (MECE)

| 유형 | 신원의 주인 | 쓰는 곳 | 비밀 |
| --- | --- | --- | :---: |
| **관리 ID** (Managed Identity) | **Azure 리소스**(VM·App Service·Functions) | 리소스가 곧 주체일 때 | 없음 |
| **워크로드 ID** (Workload Identity) | **Kubernetes ServiceAccount** | **AKS 안의 앱** | 없음 |
| 시크릿·액세스 키 | 값 그 자체 | Entra 미지원 외부 시스템 | **있음** |

> ⚠️ **AKS 에서 노드(kubelet)의 관리 ID 를 앱 권한으로 쓰면 안 됩니다.** 같은 노드에 뜬 **다른 파드도 같은 권한**을 갖게 됩니다. 파드 단위로 신원을 분리하려고 워크로드 ID 가 따로 존재합니다.

### 6-9-3. 워크로드 ID 가 작동하는 원리 — 다섯 가지가 모두 맞아야 합니다

```
  ① AKS OIDC 발급자 ON      ─┐
  ② 워크로드 ID 애드온 ON    ─┤   웹훅이 파드에 주입
  ③ 연합 자격 증명           ─┤   AZURE_CLIENT_ID / AZURE_TENANT_ID
     issuer = ①의 URL        │   AZURE_FEDERATED_TOKEN_FILE / AUTHORITY_HOST
     subject = system:serviceaccount:<ns>:<sa>
  ④ ServiceAccount 주석 = 관리 ID clientId
  ⑤ 파드 라벨 azure.workload.identity/use="true"
                 │
                 ▼
  파드가 SA 토큰을 제출 → Entra 가 연합 확인 → 액세스 토큰 발급 → 리소스 RBAC 확인
```

| 빠진 것 | 증상 |
| --- | --- |
| ① 또는 ③ | `AADSTS70021` — 연합 자격 증명 일치 실패 |
| ② 또는 ⑤ | 환경 변수·토큰 파일이 **아예 주입되지 않음** |
| ④ | `AZURE_CLIENT_ID` 미주입 |

> 🔎 **`subject` 는 정확히 `system:serviceaccount:<네임스페이스>:<서비스계정>` 입니다.** 오타 한 글자면 실패하는데 오류는 «권한 없음»처럼 보입니다. 그래서 스크립트는 이 문자열을 [`Get-FederatedSubject`](../배포/common/identity.ps1) **한 곳에서만** 만듭니다.

### 6-9-4. 리소스별 «비밀 없는» 접근 — prd 에 실제 적용된 것

| 리소스 | 지양 | **채택** | 부여 역할 |
| --- | --- | --- | --- |
| **ACR** | `imagePullSecret` | AKS kubelet 관리 ID | `AcrPull` |
| **Key Vault** | 앱에 시크릿 보관 | 워크로드 ID + CSI | `Key Vault Secrets User` (**읽기만**) |
| **Storage** | 계정 키 · SAS | 공유 키 비활성 + RBAC | `Storage Blob Data Contributor` |
| **PostgreSQL** | 관리자 비밀번호 | **Entra ID 인증 + 토큰** | Entra DB 관리자 |
| **Redis** | 액세스 키 | **Entra 액세스 정책** | `Data Contributor` |

```powershell
# 20_config.ps1 이 실제로 실행하는 명령 (az CLI 2.60.0 에서 존재 확인 완료)
az aks create ... --enable-oidc-issuer --enable-workload-identity --attach-acr <acr>
az identity create -g <rg> -n id-myapp-prd
az identity federated-credential create -g <rg> --identity-name id-myapp-prd -n fc-myapp `
    --issuer <OIDC URL> --subject system:serviceaccount:myapp-prd:myapp-sa `
    --audiences api://AzureADTokenExchange
az postgres flexible-server update -g <rg> -n <pg> --active-directory-auth Enabled
az postgres flexible-server ad-admin create -g <rg> -s <pg> --display-name id-myapp-prd --object-id <principalId> --type ServicePrincipal
az redis access-policy-assignment create -g <rg> -n <redis> --access-policy-name "Data Contributor" --object-id <principalId> --object-id-alias id-myapp-prd
az storage account create ... --allow-shared-key-access false
```

> 🚫 **역할은 «읽기만 필요하면 읽기만»** — `Key Vault Secrets Officer`(쓰기·삭제 포함)나 `Storage Account Contributor`(**계정 키 조회 가능**)를 주면, 공유 키를 비활성화한 의미가 사라집니다.
> 🚫 **구독 범위 역할은 금지** — 하나가 뚫리면 전부 뚫립니다. `80_verify.ps1` 이 이를 **실제로 검사**합니다(P-23).

### 6-9-5. 앱은 어떻게 바뀌나 — 이미지는 하나 그대로

```javascript
// 배포/common/app/src/lib/authmode.js — 환경이 스스로 인증 방식을 고른다
function resolveAuthMode(env) {
  const explicit = String(env.DB_AUTH_MODE || "").toLowerCase();
  if (explicit === "entra" || explicit === "password") return explicit;
  if (workloadIdentityAvailable(env) && !env.DB_PASSWORD) return "entra";
  return "password";
}
```

```javascript
// 배포/common/app/src/db.js — pg 는 password 로 '함수'를 받을 수 있다
if (mode === "entra") {
  cfg.password = async () => getAccessToken();  // 연결할 때마다 최신 토큰
  cfg.ssl = { rejectUnauthorized: false };
}
```

| 환경 | `DB_PASSWORD` | 워크로드 ID | 선택되는 모드 |
| --- | :---: | :---: | --- |
| dev (k3d) | 있음 | 없음 | `password` |
| stg (AKS + 컨테이너 DB) | 있음 | 전제만 켬 | `password` |
| **prd (AKS + PaaS DB)** | **없음**(암호 인증 해제 시) | **있음** | **`entra`** |

> 🔑 **이미지도 매니페스트도 바꾸지 않습니다.** «같은 이미지가 모든 환경에서 동일하게 동작한다»(NFR-07)를 지키면서 인증만 승격됩니다.
> 🔎 **`@azure/identity` 를 쓰지 않은 이유** — 토큰이 «어디서 어떻게 나오는지»를 보이게 하는 것이 학습 목표이고, 의존성이 없으면 폐쇄망에서도 실습이 됩니다. 운영 프로젝트에서는 라이브러리를 권장합니다.

### 6-9-6. 검증 — «적용했다»가 아니라 «작동한다»

| 환경 | 검증 항목 | 무엇을 보나 |
| --- | --- | --- |
| **stg** | **G-06~G-09** | OIDC 발급자 · WI 애드온 · kubelet 관리 ID · **`imagePullSecret` 미사용** |
| **prd** | **P-16~P-23** | 위 + CSI 애드온 · 연합 일치 · SA 주석 · **파드 토큰 주입** · PostgreSQL Entra · **구독 범위 권한 0건** |
| **모든 환경** | **U-A-07~10** | 인증 방식 선택 로직 · **진단 응답에 비밀 미노출** |
| **모든 환경** | **I-A-10** | `/version` 이 **방식만** 노출 |

```powershell
# prd 검증 실행 — 리소스와 클러스터를 실제로 조회해서 판정한다
cd C:\dev\workspace-igm\advanced-cloud-developer\배포\prd
pwsh -File .\80_verify.ps1
```

> 🎯 **P-21(파드 토큰 주입)이 분기점입니다.** P-16~P-20 은 «설정이 되어 있다»를 보지만, P-21 은 파드 안에서 `printenv` 로 **토큰이 실제 주입되었는지**를 확인합니다. 설정은 다 맞는데 **파드 라벨 하나가 빠져** 웹훅이 건너뛴 경우, 이것만이 잡아냅니다.

### 6-9-7. ▶ 프롬프트 — 내 앱에 아이덴티티 적용하기

```text
내 앱(실습산출물/3차시/myapp)에 관리 ID · 워크로드 ID 기반 '비밀 없는 접근'을 적용해줘.

[원칙]
- 비밀을 '잘 보관'하는 방법을 찾기 전에, '만들지 않는' 방법이 있는지 먼저 검토할 것.
- 역할은 리소스 범위 · 최소 권한으로만 부여할 것. 구독 범위 부여는 금지.
- 관리 역할(*Contributor)로 데이터에 접근하지 말 것 — 데이터 평면 역할을 쓸 것.
- 적용만 하지 말고 반드시 '작동하는지' 검증 코드까지 만들 것.

[단계]
1) 내 앱이 접근하는 Azure 리소스를 모두 나열하고, 각각에 대해
   (a) Entra 인증을 지원하는가  (b) 지원한다면 필요한 역할이 무엇인가 를 표로 정리해줘.
2) 지원하는 리소스는 배포/common/identity.ps1 의 함수를 재사용해
   배포/prd/20_config.ps1 에 적용 코드를 추가해줘.
3) 지원하지 않는 리소스만 Key Vault 에 남기고, 회전 절차를 문서화해줘.
4) 앱 코드는 배포/common/app/src/lib/authmode.js 와 같은 방식으로
   '환경이 스스로 인증 방식을 고르게' 만들어줘 — 이미지는 하나로 유지할 것.
5) 배포/prd/80_verify.ps1 에 검증 항목을 추가하고, 파드 안에서 토큰 주입까지 확인할 것.

[검증]
- 인수조건: (a) 앱 코드·Git·이미지에 비밀 값이 없음
  (b) 구독 범위 역할 할당 0건  (c) 80_verify 아이덴티티 항목 전부 Pass
  (d) /version 응답에 자격 증명 값이 없음
- 4가지를 O/X 표로 보고하고, 남은 비밀이 있다면 '왜 남길 수밖에 없는지'를 설명해줘.
```

---

## 6-10. 설계 산출물 지도 — «환경 불변» 설계와 «환경별» 설계

`배포/` 가 **«어떻게 실행하는가»**라면, [`설계/`](../설계/README.md) 는 **«왜 그렇게 만들었는가»**를 담습니다.

### 6-10-1. 설계를 두 덩어리로 나눈 기준 (MECE)

```
                 ┌────────────────────────────────────────────┐
                 │  설계/공통/   «무엇을 만드는가»              │
                 │  환경이 바뀌어도 동일 — 한 번만 정의한다      │
                 │  기능 · 도메인/클래스 · API 계약 ·           │
                 │  시퀀스/상태 · 데이터 모델 · 테스트 케이스     │
                 └──────────────────┬─────────────────────────┘
                                    │ 참조(단일 원천)
          ┌─────────────────────────┼─────────────────────────┐
          ▼                         ▼                         ▼
   설계/dev  «내 코드가       설계/stg  «배포 방식이     설계/prd  «장애가 나도
             도는가»                    맞는가»                   살아남는가»
   ↳ 요약은 위 6-3 절        ↳ 요약은 위 6-4 절        ↳ 요약은 위 6-5 절
```

| 구분 | 담는 것 | 담지 않는 것 | 이 문서에서의 위치 |
| --- | --- | --- | --- |
| **공통** | 기능·도메인·API 계약·시퀀스·데이터 모델·TC **정의** | 인프라·배포 방식·환경 값 | **아래 6-9-2** |
| **환경별** | 아키텍처(토폴로지)·배포 프로세스·구성 값·테스트 **범위** | 기능 정의·API 계약(공통을 참조만) | **위 6-3 / 6-4 / 6-5 «📐 설계 요약»** |

> 📌 **같은 내용을 두 번 쓰지 않는 것이 MECE 의 핵심**입니다. 예를 들어 «항목 등록 API 는 201 을 반환한다»는 [공통 03](../설계/공통/03_REST_API_설계서.md)에만 있고, «prd 에서는 그 API 의 **쓰기 TC 를 실행하지 않는다**»는 [prd 04](../설계/prd/04_테스트설계서_prd.md)에만 있습니다.

### 6-10-2. 공통 설계 7종 — 환경이 바뀌어도 그대로

| 번호 | 문서 | 답하는 질문 | 대표 산출 |
| --- | --- | --- | --- |
| [01](../설계/공통/01_기능명세서.md) | **기능명세서** | «무엇이 합격인가» | FR-A/B · NFR · **인수조건(AC)** |
| [02](../설계/공통/02_도메인·클래스설계서.md) | **도메인 · 클래스 설계서** | «코드를 어떻게 나누는가» | **클래스 다이어그램** · 계층 의존 규칙 |
| [03](../설계/공통/03_REST_API_설계서.md) | **REST API 설계서** | «어떤 계약으로 주고받는가» | 엔드포인트 · 상태 코드 결정 흐름 |
| [04](../설계/공통/04_시퀀스·상태설계서.md) | **시퀀스 · 상태 설계서** | «언제 무엇이 어떤 순서로» | **시퀀스 다이어그램** · 테트리스 상태 기계 |
| [05](../설계/공통/05_데이터모델설계서.md) | **데이터 모델 설계서** | «무엇을 어떻게 저장하는가» | **ERD** · DDL · 스키마 진화 5단계 |
| [06](../설계/공통/06_테스트설계서.md) | **테스트 설계서** | «무엇을 어떤 수준에서 검증하는가» | **TC 카탈로그** · AC↔TC 추적성 |
| [07](../설계/공통/07_아이덴티티·시크릿_설계서.md) | **아이덴티티 · 시크릿 설계서** | «앱은 자기를 어떻게 증명하고 비밀은 어디에 두는가» | 신원 유형 분류 · **금지 목록** · ID-01~12 |

**두 앱을 함께 다룹니다**

| 코드 | 앱 | 특성 | 설계상 초점 |
| --- | --- | --- | --- |
| **APP-A** | **나만의 업무 앱** | 상태 있음 · PostgreSQL | 데이터 모델 · API 계약 · 통합 테스트 |
| **APP-B** | **웹 테트리스 게임** | 상태 없음 · 정적 서빙 | 상태 기계 · 순수 로직 단위 테스트 · E2E |

> 🔎 **성격이 다른 두 앱을 함께 설계하는 이유** — «DB 가 있는 앱»과 «없는 앱»은 **테스트 전략이 정반대**입니다. APP-A 는 통합 테스트가, APP-B 는 단위 테스트가 무게중심입니다. 하나만 다루면 이 차이를 배울 수 없습니다.

### 6-10-3. 상황별로 어느 문서를 보나

| 지금 하려는 일 | 볼 문서 |
| --- | --- |
| «무엇을 만들면 합격인가» 확정 | [공통 01 기능명세서](../설계/공통/01_기능명세서.md) |
| 코드를 어떤 파일·클래스로 나눌지 | [공통 02 도메인·클래스](../설계/공통/02_도메인·클래스설계서.md) |
| API 응답 형태·상태 코드 결정 | [공통 03 REST API](../설계/공통/03_REST_API_설계서.md) |
| 테이블·컬럼·인덱스 설계 | [공통 05 데이터 모델](../설계/공통/05_데이터모델설계서.md) |
| 테스트를 어느 수준에서 쓸지 | [공통 06 테스트설계서](../설계/공통/06_테스트설계서.md) |
| `배포/dev` 스크립트가 막혔을 때 | [dev 02 프로세스](../설계/dev/02_프로세스설계서_dev.md) · [dev 03 구성](../설계/dev/03_구성설계서_dev.md) |
| **`env.*.json` 값을 바꾸기 전** | 해당 환경 **03 구성 설계서 §«설정 변경 영향 범위»** |
| Argo CD 가 동기화되지 않을 때 | [stg 02 프로세스](../설계/stg/02_프로세스설계서_stg.md) · [stg 04 테스트](../설계/stg/04_테스트설계서_stg.md) |
| Key Vault CSI 마운트가 실패할 때 | [prd 03 구성 §3](../설계/prd/03_구성설계서_prd.md) |
| 운영에서 장애가 났을 때 | [prd 05 운영·이행 §3 런북](../설계/prd/05_운영·이행설계서_prd.md) |
| **워크로드 ID 토큰 교환이 실패할 때** | [공통 07 §3-1 성립 조건 5가지](../설계/공통/07_아이덴티티·시크릿_설계서.md) · 위 **6-9-3** |
| **어떤 역할을 줘야 할지 모를 때** | [공통 07 §4-1 역할 선택 규칙](../설계/공통/07_아이덴티티·시크릿_설계서.md) |

> ⚠️ **`env.<env>.json` 을 고치기 전에 03 구성 설계서를 먼저 보세요.** 예를 들어 dev 의 `cluster.hostPort` 만 바꾸고 `test.baseUrl` 을 그대로 두면 **배포는 성공하는데 통합·E2E 가 전부 실패**합니다. 각 03 문서의 «설정 변경 영향 범위» 표가 이런 짝을 정리해 둔 곳입니다.

### 6-10-4. 설계 ↔ 실행 대응 — 문서와 스크립트는 짝이다

| 설계 문서 | 대응 스크립트·파일 | 어긋나면 생기는 일 |
| --- | --- | --- |
| 환경별 **01 아키텍처** | `20_config.ps1` · `config/k8s/**` | 그림과 실제 리소스가 달라 진단이 헛돈다 |
| 환경별 **02 프로세스** | `30_build` · `40_deploy` · `rollback` | «왜 이 순서인지» 몰라 단계를 건너뛴다 |
| 환경별 **03 구성** | `config/env.*.json` · ConfigMap/Secret | 값 하나만 바꾸고 짝을 놓쳐 테스트가 깨진다 |
| 환경별 **04 테스트** | `50`·`60`·`70`·`80` 스크립트 | 건너뛴 TC 를 «통과»로 착각한다 |
| 공통 **06 테스트** | `test/unit · integration · e2e` | AC 는 있는데 검증하는 TC 가 없다 |

> 🔑 **설계서를 고쳤으면 스크립트도, 스크립트를 고쳤으면 설계서도** 함께 갱신하세요. 이 과정에서 만든 설계서는 **`배포/` 의 실제 값을 정본으로** 작성되었습니다.

### 6-10-5. ▶ 프롬프트 — 내 주제에 맞게 설계서 갱신하기

```text
설계/ 폴더의 산출물을 내 앱 주제에 맞게 갱신해줘.

[내 주제]
- 앱 이름: (예: 회의록 정리 도우미)
- 핵심 기능 3가지: (예: 회의록 등록 / 주차별 요약 / 액션아이템 추출)

[갱신 대상 — 공통(환경 불변)부터]
1) 설계/공통/01_기능명세서.md    → FR-A-*, AC-A-* 를 내 기능으로 교체 (ID 체계는 유지)
2) 설계/공통/03_REST_API_설계서.md → 엔드포인트를 내 기능에 맞게 재정의
3) 설계/공통/05_데이터모델설계서.md → items 테이블 컬럼을 내 도메인에 맞게 조정
4) 설계/공통/06_테스트설계서.md   → 변경된 AC 에 대응하는 TC 를 추가/수정하고
                                   AC↔TC 추적성 표를 100% 로 다시 채울 것

[갱신 대상 — 환경별]
5) 설계/dev|stg|prd/04_테스트설계서_*.md 의 «실행 범위» 표만 갱신
   - prd 는 쓰기 TC 를 반드시 '건너뜀(skip)' 으로 유지할 것
6) 값이 바뀌면 해당 환경 03_구성설계서_*.md 의 «설정 변경 영향 범위» 표도 갱신

[반드시 지킬 것]
- 환경별 문서의 «구조»(01 아키텍처 / 02 프로세스 / 03 구성 / 04 테스트)는 바꾸지 말 것.
  구조가 같아야 세 환경을 가로로 비교하며 배울 수 있다.
- 설계서의 값은 배포/*/config/ 의 실제 파일을 정본으로 삼을 것.
  문서와 스크립트가 어긋나면 스크립트 쪽을 사실로 보고 문서를 고칠 것.
- 개인정보·실제 인사 데이터는 절대 사용하지 말 것 (가상 데이터·마스킹).
- 비밀값을 문서에 예시로도 적지 말 것.

[검증]
- 인수조건: (a) AC↔TC 추적성 100%  (b) 문서의 값이 배포/*/config/ 와 일치
  (c) prd 쓰기 TC 가 skip 으로 유지됨  (d) 깨진 상대 링크 없음
- 4가지를 O/X 표로 보고하고, 변경한 문서마다 «무엇을 왜 바꿨는지» 3줄 요약을 덧붙일 것.
```


# Part G. (심화·선택) Advisor Strategy — 비싼 머리 + 싼 손으로 비용 절감

> 💡 **심화·선택 트랙**입니다(코어 120분과 별개). 위 실습을 마친 뒤, **비용 최적화 협업 패턴**과 **실제 컨테이너 배포**를 한 번에 경험하고 싶은 분을 위한 확장 과제입니다.
> ⏱️ 예상 시간 **45분**

## 7-1. 학습 목표

**고성능 모델(Advisor)** 이 분석·설계·검증을, **저비용 모델(Executor)** 이 대량 구현을 맡는 **Advisor Strategy(10‑80‑10 릴레이) 패턴**으로 웹 테트리스 게임을 **분석 → 설계 → 개발 → 테스트**하고, **Docker 이미지**로 만들어 **k3d 클러스터에 배포**해 **브라우저로 접속 가능한 서비스**로 완성합니다.

## 7-2. 🧭 쉽게 이해하기 (비유로)

**Advisor Strategy 패턴**은 **«값비싼 베테랑 설계자 + 성실한 실무 시공팀»** 의 협업입니다.

| 역할 | 비유 | 모델 예시 | 담당 |
| --- | --- | --- | --- |
| **Advisor** | 베테랑 건축가 | 고성능 모델 · 생각 강도 높게 | 설계도·검수 기준 확정(분석·설계), 완공 후 **하자 점검**(검증) |
| **Executor** | 성실한 시공팀 | 저비용 모델 · 생각 강도 낮게 | 설계도대로 **대량으로 빠르고 싸게** 구현 |

```
   설계·판단 10%        대량 구현 80%         최종 검증 10%
   ─────────          ─────────           ─────────
   Advisor(고성능)  ▶  Executor(저비용)  ▶  Advisor(고성능)
   DESIGN.md           engine.js·Docker      TEST_REPORT.md
```

> **10‑80‑10** = 설계·핵심판단 **10%(Advisor)** → 대량 구현 **80%(Executor)** → 최종 검증 **10%(Advisor)**.
> *비싼 사람은 머리 쓰는 데, 싼 사람은 손 쓰는 데* 쓰면 **품질은 지키고 비용은 크게 절감**됩니다.

> 🔎 **모델 전환**: 세션에서 `/model`로 단계마다 바꾸거나, `/agents`로 **역할별 서브에이전트에 모델을 지정**합니다. 단계 전후 `/cost`로 **토큰 사용량을 비교**해 절감 효과를 확인하세요.

## 7-3. ▶ 심화 통합 프롬프트 (4단계 · 모델을 바꿔 가며 실행)

> ⚠️ 각 단계 시작 전에 `/model`로 표시된 모델(Advisor/Executor)로 **바꾼 뒤** 해당 블록을 실행하세요.

```text
웹 테트리스 게임을 Advisor Strategy(10-80-10)로 만들어 k3d에 배포해줘.
모든 산출물은 실습산출물/3차시/tetris/ 아래에 저장하고, 각 단계 끝에 "입력 → 산출물"을 보고해.

[1단계 · 분석·설계]  ← /model 로 Advisor(고성능·고강도) 선택
  목적: '검증 가능한 명세와 설계도'를 먼저 확정 (구현하지 말 것)
  입력: 없음
  작업: ① 게임 규칙 명세 — 7종 테트로미노(I·O·T·S·Z·J·L), SRS 회전+월킥, 중력/소프트드롭/하드드롭,
           라인 클리어(1~4줄)와 점수·레벨·속도, 다음 블록·홀드·일시정지·게임오버/재시작,
        ② 테스트 가능한 구조 — '순수 로직(보드·충돌·회전·라인클리어·점수)'과 '렌더/입력(Canvas·키보드)' 분리 + 상태 다이어그램,
        ③ 기술 선택 — 빌드 없이 도는 HTML5 Canvas + 바닐라 JS, nginx 정적 서빙(근거 명시),
        ④ 인수조건(Given-When-Then) 8개 (정상 5 + 예외 3) — 예: 벽/바닥/블록 충돌, 라인 수별 점수, 스폰 충돌=게임오버.
  산출물: tetris/DESIGN.md   ← 이후 구현·검증의 '합격 기준'

[2단계 · 개발]  ← /model 로 Executor(저비용·저강도) 선택
  목적: 설계도대로 '그대로 시공' (설계를 벗어나지 말 것)
  입력: tetris/DESIGN.md
  작업: ① 순수 로직을 tetris/src/engine.js 로 분리 구현(DOM/Canvas 의존 없이 테스트 가능하게),
        ② 렌더/입력을 tetris/src/main.js + index.html 로(Canvas 렌더 루프, 키보드 조작, 다음 블록·점수·레벨 표시, 게임오버/재시작),
        ③ 컨테이너화 — nginx:alpine 정적 서빙 Dockerfile + .dockerignore,
        ④ 로컬 확인 방법·조작법을 tetris/README.md 에,
        ⑤ 인수조건을 어디서 충족하는지 주석/문서로 표시.
  산출물: tetris/src/ · index.html · Dockerfile · README.md
  ※ 비용의 대부분이 이 단계 — 저비용 모델로 처리해 절감

[3단계 · 테스트·검증]  ← /model 로 Advisor(고성능·고강도) 복귀
  목적: 최종 품질 게이트
  입력: DESIGN.md + tetris/src/engine.js
  작업: ① 순수 로직 단위 테스트 작성(Jest 또는 node assert) — 충돌, 회전+월킥(벽/바닥/블록 경계), 라인 클리어(1~4줄)와 점수, 게임오버,
        ② 테스트를 실제 실행해 통과/실패를 표로 보고하고, 실패는 원인과 함께 최소 수정,
        ③ DESIGN.md 인수조건 8개를 '충족/미충족(근거)'로 체크,
        ④ 흔한 결함 집중 점검 — 회전 시 벽 뚫림, 라인 인덱스 꼬임, 하드드롭 위치 오류, 게임오버 미감지.
  산출물: tetris/TEST_REPORT.md   (초록불이 될 때까지 ②~④ 반복)

[4단계 · 배포]  ← /model 로 Executor(저비용) 선택 (정형 작업)
  목적: 브라우저로 접속 가능한 서비스로
  입력: 검증 통과한 tetris/
  작업: ① docker info 로 엔진 확인, k3d 클러스터(있으면 재사용)와 네임스페이스 tetris 준비,
        ② 이미지 빌드 → k3d image import (레지스트리 없이),
        ③ Deployment(replicas 2, requests/limits, liveness·readiness)와 Service(ClusterIP 80) 배포,
        ④ port-forward 로 브라우저 접속(http://localhost:8081) 후 실제 플레이로 확인,
        ⑤ 매니페스트는 tetris/k8s/ 에, 빌드·배포·접속·정리 절차를 tetris/DEPLOY.md 로.
  산출물: tetris/k8s/ · DEPLOY.md
  ※ 파괴적 명령(삭제)은 먼저 확인받아. 배포 후 /cost 로 Advisor/Executor 분담 효과를 한 줄 회고로 남겨줘.
```

**수동 실행 (참고 명령)**

```powershell
cd "C:\dev\workspace-igm\advanced-cloud-developer\실습산출물\3차시\tetris"
claude
#   1·3단계(설계·검증) → /model 로 고성능(Advisor)
#   2·4단계(구현·배포) → /model 로 저비용(Executor)
kubectl port-forward -n tetris svc/tetris 8081:80     # 브라우저 http://localhost:8081
```

> 💡 **팁**: `/agents`로 `game-architect`(Advisor)·`game-builder`(Executor) 서브에이전트에 **모델을 지정**해 두면 전환이 자동화됩니다.

## 7-4. 심화 확인 포인트

| 확인 항목 | 무엇을 보나 |
| --- | --- |
| **산출물 순서** | `tetris/`에 **DESIGN → 코드 → TEST_REPORT → k8s 매니페스트**가 순서대로 남고, **인수조건이 초록불**인가 |
| **실제 동작** | 파드가 Running이고 **브라우저에서 실제로 플레이**되는가 |
| **비용 절감** | `/cost`로 **Advisor(설계·검증) vs Executor(구현·배포)** 분담에 따른 **비용 차이**를 확인했는가 |

> 📌 **참고(출처)**: 본 실습의 협업 방식은 유튜브 채널 *Advisor Strategy* — "클로드 Fable 5 비용 아끼는 법"에서 소개된 **Advisor/Executor 릴레이(10‑80‑10)** 비용 최적화 패턴을 실무 개발·배포에 적용한 것입니다.

---

# Part H. 마무리 — 회고 & 리소스 정리

> 🚨 **이 파트는 선택이 아니라 필수입니다.** 컨테이너·클러스터는 켜 둔 만큼 PC 자원(또는 클라우드 비용)을 씁니다.

## 8-1. ▶ 프롬프트 — 과정 회고

```text
실습산출물 폴더 전체(1차시·3차시)를 훑어보고 과정 회고 문서를 만들어줘.
[원칙]
- 실제로 존재하는 파일만 근거로 삼고, 없는 산출물은 '미완료'로 표시해.
[단계]
1) 차시별 핵심 학습과 만든 산출물을 표로 정리하고,
2) 내가 현업에 바로 적용할 3가지를 'ROI · 난이도' 기준으로 우선순위화하고,
3) 더 공부하면 좋을 주제 3가지를 추천하고,
4) 우리 팀에 이 방식을 확산할 때의 체크리스트(보안 · 권한 · 교육 · 비용)를 만들어줘.
[산출물]
- 실습산출물/3차시/05_과정회고.md
[검증]
- 표에 실제 파일 경로가 들어갔는가 · 미완료 항목이 명확히 구분됐는가
```

## 8-2. 리소스 정리

```powershell
# 실행 중인 컨테이너·클러스터 확인 (삭제 전 필수)
docker ps
k3d cluster list

# 이번 실습에서 만든 것만 정리 (⚠️ 이름을 반드시 확인하고 실행)
kubectl delete namespace tetris          # 심화 트랙을 진행한 경우
k3d cluster delete <클러스터이름>          # 이 실습용으로 새로 만든 클러스터만
docker image prune -f                    # 사용하지 않는 이미지 정리
```

> ⚠️ **삭제는 되돌릴 수 없습니다.** 다른 용도로 쓰던 클러스터·이미지가 섞여 있지 않은지 **이름을 먼저 확인**하세요. *(1차시 Part G의 «목록 확인 → 승인 → 실행» 원칙과 동일합니다.)*

---

## 정리 & 체크리스트

### Part A~B — 준비 · 주제 확정

| 확인 | 상태 |
| --- | --- |
| 작업 폴더(`실습산출물\3차시\myapp` · `mockup` · `캡처`) 생성 | ⬜ |
| 도구 점검: Node · Docker(또는 대체) · MCP 4종 · xlsx | ⬜ |
| **【나만의 앱 주제】** 를 내가 직접 정했다 (추천 4개 중 택1 또는 본인 업무) | ⬜ |
| **【데이터 소스】** 를 정했다 ((a)입력 / (b)공개 수집 / (c)가상) | ⬜ |
| **민감 주제(HR 등)는 가상 데이터로** 진행 · 실제 개인정보 미사용 | ⬜ |

### Part C~D — 6단계 실행

| 확인 | 단계 | 상태 |
| --- | --- | --- |
| 인수조건 6개 + **집계 기준(주간)** + 실무 요건 + 스타터 비교 | ① 스펙 | ⬜ |
| 화면·**차트/리포트 레이아웃**·데이터 모델·API·기술스택 확정 | ② 설계 | ⬜ |
| **HTML 목업**을 만들어 브라우저로 확인 · 캡처 `mockup_01~03` 저장 | ② 설계 | ⬜ |
| **목업 화면 구조를 이어받아** `myapp/`이 실행·즉시 시연 | ③ 구현 | ⬜ |
| **데이터 파이프라인**: 소스대로 동작(수집 실패·0건 처리 포함) | ③ 구현 | ⬜ |
| **산출물 생성**: 차트 렌더링 또는 리포트(문서/PPT) 생성 확인 | ③ 구현 | ⬜ |
| 실무 요건: 저장·내보내기·권한·입력검증·감사로그 동작 | ③ 구현 | ⬜ |
| **엑셀 테스트 시나리오**(`03_테스트시나리오.xlsx` · 4시트) 작성 | ④ 테스트 | ⬜ |
| Playwright E2E 성공 화면이 `캡처/`에 저장 (01~04) | ④ 테스트 | ⬜ |
| **엑셀 결과(Pass/Fail) 열이 실행 결과로 채워짐** · [요약] 커버리지 | ④ 테스트 | ⬜ |
| 인수조건 6개 대비 통과/실패 표 · **캡처·엑셀에 개인정보 없음** | ④ 테스트 | ⬜ |
| README만 보고 실행 가능 + 3분 시연 시나리오 | ⑤ 시연·배포 | ⬜ |
| **산출물 체인**(주제·소스→스펙→설계→앱→테스트→시연) 확인 | 전체 | ⬜ |
| *(③ 증시 주제)* 출처·수집시각 표기 · "투자 조언 아님" 명시 | 전체 | ⬜ |

### Part F — 배포 파이프라인 (dev · stg · prd)

| 확인 | 환경 | 상태 |
| --- | --- | --- |
| **6-3 «📐 dev 설계 요약»** 을 읽고 «왜 복제본 1인가»를 설명할 수 있다 | dev | ⬜ |
| `10_prereq` 전 항목 Pass (Docker·k3d·kubectl·node) | dev | ⬜ |
| k3d 클러스터 구성 · in-cluster PostgreSQL Running | dev | ⬜ |
| 빌드(git SHA 태그) → `k3d image import` → 배포 성공 | dev | ⬜ |
| <http://localhost:8080> 접속 확인 | dev | ⬜ |
| 단위 · 통합 · **E2E 3단계 모두 Pass** + 캡처 생성 | dev | ⬜ |
| `80_verify` — **배포 버전 = 빌드 태그 일치** | dev | ⬜ |
| **클러스터 정리 완료**(`90_cleanup`) | dev | ⬜ |
| **6-4 «📐 stg 설계 요약»** 을 읽고 Push 와 Pull 배포의 차이를 설명할 수 있다 | stg | ⬜ |
| `acrName`(전역 고유) · `repoUrl`(Git 저장소) 변경 · push 완료 | stg | ⬜ |
| AKS·ACR 생성 · **Argo CD 설치** · Application 등록 | stg | ⬜ |
| `az acr build` 푸시 → **overlay 태그 커밋 → Synced/Healthy** | stg | ⬜ |
| 단위 · 통합 · E2E 3단계 모두 Pass | stg | ⬜ |
| **selfHeal 드리프트 복구 검증 통과** | stg | ⬜ |
| **리소스 그룹 삭제 완료** | stg | ⬜ |
| **6-5 «📐 prd 설계 요약»** 을 읽고 비밀이 파드에 도달하는 경로를 설명할 수 있다 | prd | ⬜ |
| stg 검증 통과 기록 확인 후 진행 | prd | ⬜ |
| 네트워크→관측→보안→**PaaS 데이터**→AKS 순서로 구성 | prd | ⬜ |
| 노드·파드가 **2개 이상 가용성 영역**에 분산 | prd | ⬜ |
| **불변 태그**(`prd-<sha>`) 빌드 · **수동 Sync 승인** 배포 | prd | ⬜ |
| **Key Vault → Secret 주입 확인** · PaaS DB `/readyz` 200 | prd | ⬜ |
| **6-9 «보안 강화»** 를 읽고 관리 ID 와 워크로드 ID 의 차이를 설명할 수 있다 | prd | ⬜ |
| **워크로드 ID 성립 5조건**이 모두 충족됨(P-16~P-20) | prd | ⬜ |
| **파드 안에서 토큰 주입 확인**(P-21) — 설정이 아니라 «작동»을 확인 | prd | ⬜ |
| **구독 범위 역할 할당 0건**(P-23) · 데이터 평면 역할만 사용 | prd | ⬜ |
| E2E 스모크 **읽기 전용**으로 Pass | prd | ⬜ |
| `80_verify` 가용성·보안·데이터·관측·복구 전 항목 Pass | prd | ⬜ |
| **롤백 연습 수행**(`rollback.ps1`) | prd | ⬜ |
| **리소스 그룹 + Key Vault purge 완료** | prd | ⬜ |

### Part E · G · H — 해설 · 심화 · 마무리

| 확인 | 상태 |
| --- | --- |
| 통합 프롬프트의 각 줄이 **5블록 중 어디에 해당하는지** 설명할 수 있다 | ⬜ |
| *(선택)* Advisor Strategy 4단계 완료 · `/cost`로 비용 차이 확인 | ⬜ |
| 과정 회고 문서(`05_과정회고.md`) 작성 | ⬜ |
| **컨테이너·클러스터 리소스 정리 완료** | ⬜ |

### 산출물 목록 (`실습산출물\3차시\`)

| 파일 | 내용 |
| --- | --- |
| `00_환경점검.md` | 폴더·도구 준비 상태 점검 리포트 |
| `01_앱기획_스펙.md` | 사용자 스토리 · KPI · 실무 요건 · 집계 기준 · **인수조건 6개** |
| `02_앱설계.md` | 화면·데이터 모델·API·기술스택·시퀀스 다이어그램 |
| `myapp/mockup/` | HTML 목업 · Dockerfile · k8s 매니페스트 |
| `캡처/mockup_01~03_*.png` | 목업 화면 캡처 |
| `myapp/` | 앱 코드 · `README.md` · 샘플 데이터 · (리포트형) 리포트 샘플 |
| `03_테스트시나리오.xlsx` | **단위 · 통합 · E2E · 요약 4시트** · 결과(Pass/Fail) |
| `03_테스트결과.md` | 인수조건 6개 대비 통과/실패 표 |
| `캡처/01~04_*.png` | E2E 성공·예외 화면 캡처 |
| `04_시연·배포가이드.md` | 3분 시연 시나리오 · 배포 절차 |
| `05_과정회고.md` | 차시별 학습·산출물 정리 · 현업 적용 우선순위 |
| *(심화)* `tetris/` | `DESIGN.md` · `src/` · `TEST_REPORT.md` · `k8s/` · `DEPLOY.md` |
| `06_배포스크립트_조정.md` | 내 앱에 맞게 배포 스크립트를 고친 내역(Part F 6-7) |

### 배포 산출물 (`배포\<env>\reports\`)

| 파일 | 내용 |
| --- | --- |
| `<env>_prereq_*.json` | 전제조건 점검 결과 |
| `last-build.json` | 직전 빌드 이미지·태그·git sha |
| `<env>_unit / integration / e2e_*.json` | **테스트 3단계 결과** |
| `<env>_verify_*.json` | 환경별 검증 결과 — **승격 게이트** |
| `<env>_pipeline_*.json` | 전체 파이프라인 요약 |
| `배포\prd\reports\infra-state.json` | Key Vault·clientId·DB 호스트 등 인프라 상태 |
| `<앱>\test\e2e\캡처\<env>_01~04_*.png` | 환경별 E2E 화면 캡처 |

### 설계 산출물 (`설계\`) — 21종

| 구분 | 문서 | 무엇을 답하나 |
| --- | --- | --- |
| 지도 | [`README.md`](../설계/README.md) | 산출물 지도 · 추적성 · 읽는 순서 |
| **공통** | [01 기능명세서](../설계/공통/01_기능명세서.md) · [02 도메인·클래스](../설계/공통/02_도메인·클래스설계서.md) · [03 REST API](../설계/공통/03_REST_API_설계서.md) · [04 시퀀스·상태](../설계/공통/04_시퀀스·상태설계서.md) · [05 데이터모델](../설계/공통/05_데이터모델설계서.md) · [06 테스트](../설계/공통/06_테스트설계서.md) · **[07 아이덴티티·시크릿](../설계/공통/07_아이덴티티·시크릿_설계서.md)** | **«무엇을 만드는가»** — 환경이 바뀌어도 동일 |
| **dev** | [01 아키텍처](../설계/dev/01_아키텍처설계서_dev.md) · [02 프로세스](../설계/dev/02_프로세스설계서_dev.md) · [03 구성](../설계/dev/03_구성설계서_dev.md) · [04 테스트](../설계/dev/04_테스트설계서_dev.md) | «내 코드가 도는가» |
| **stg** | [01](../설계/stg/01_아키텍처설계서_stg.md) · [02](../설계/stg/02_프로세스설계서_stg.md) · [03](../설계/stg/03_구성설계서_stg.md) · [04](../설계/stg/04_테스트설계서_stg.md) | «배포 방식이 맞는가» |
| **prd** | [01](../설계/prd/01_아키텍처설계서_prd.md) · [02](../설계/prd/02_프로세스설계서_prd.md) · [03](../설계/prd/03_구성설계서_prd.md) · [04](../설계/prd/04_테스트설계서_prd.md) · [05 운영·이행](../설계/prd/05_운영·이행설계서_prd.md) | «장애가 나도 살아남는가» |

> 📐 **설계서의 모든 값은 `배포/*/config/` 의 실제 파일을 정본으로** 작성되었습니다. 스크립트를 고치면 설계서도 함께 갱신하세요(6-9-4 참조).

> 🧭 **오늘 남는 것**: **내 주제로, 내 손으로** 앱을 하나 완성했습니다. **주제와 데이터 소스(빈칸 2개)만 바꾸면** 이 6단계와 통합 프롬프트는 **내 어떤 반복 업무에도** 그대로 적용됩니다 — 교육이 끝나도 **혼자 이어갈 수 있습니다.**

### 다음 단계

- ◀ [1차시 실습 — 준비 및 기본 실습](1차시_준비_및_기본_실습_프롬프트.md) *(도구·Azure 기본 실습)*
- ▶ **현업 적용** — `05_과정회고.md`에서 우선순위 1위로 꼽은 업무에 이 6단계를 그대로 적용해 보세요.
- 📘 배포 상세 가이드: [`배포/가이드/배포_가이드_dev_stg_prd.md`](../배포/가이드/배포_가이드_dev_stg_prd.md) · [`배포/README.md`](../배포/README.md)
- 📐 설계 산출물: [`설계/README.md`](../설계/README.md) — 내 주제로 바꿀 때는 **공통 설계부터** 고치세요(6-9-5 프롬프트).
- 📎 관련 퀴즈: [`1차시_퀴즈10문항.docx`](../퀴즈/1차시_퀴즈10문항.docx) · [`2차시_퀴즈20문항.docx`](../퀴즈/2차시_퀴즈20문항.docx)
