# 워크로드 ID — 파드가 '비밀번호 없이' Azure 리소스(Key Vault 등)에 접근한다
apiVersion: v1
kind: ServiceAccount
metadata:
  name: myapp-sa
  namespace: myapp-prd
  annotations:
    azure.workload.identity/client-id: ea1b62d3-95d2-4924-ae46-fd23044ed0ea
