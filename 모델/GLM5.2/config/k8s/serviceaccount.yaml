# 워크로드 ID — 파드가 '비밀번호 없이' Blob·Key Vault 에 접근한다
apiVersion: v1
kind: ServiceAccount
metadata:
  name: glm-sa
  namespace: glm-serving
  annotations:
    azure.workload.identity/client-id: CLIENT_ID_PLACEHOLDER
